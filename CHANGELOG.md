# 变更日志

本项目按完整 Roadmap 阶段发布；同版本体验补丁直接修订原版本，不新增版本号。

## 2.0.0 · M8 伙伴成长与桌面形态

- 新增透明置顶桌面伙伴窗口，支持拖动、隐藏、唤回主窗口和随写作/音乐变化的轻量动作；主窗口与桌面窗口使用独立 capability。
- Rust 自定义 command 增加主窗口标签校验，桌面伙伴只能通过事件接收名称、外观和动作快照，不能调用资料库、素材、音频、密钥、备份或 vault。
- 新增可治理记忆：保存来源、时间、置信度和授权状态，支持查看、编辑、撤销、删除与 JSON 导出；删除/撤销立即停止注入。
- 新增发型/服装组合和 AI 形象“临时草稿—确认—正式素材”流程；自定义 Provider 未绑定协议时仍安全失败。
- 新增温暖、好奇、主动三项性格权重，变化保留日志并支持回退/重置；对话和音乐成长均默认关闭且需要明确授权。
- 同版本补丁修复关闭主窗口后桌面伙伴仍驻留的问题；主窗口退出现在会结束全部应用窗口。
- 同版本补丁新增由 `character.json` 驱动的差分立绘与固定 PNG 序列帧 Runtime，支持角色文件夹导入、统一画布校验、自然帧排序、眨眼、说话、待机和动作优先级，并在帮助中心加入 GPT Image 2 素材清单与提示词。

## 1.2.0 · M7 氛围与陪伴基础

- 新增受控本地音乐曲库、元数据编辑、回收站和完整备份恢复；原音乐文件不受一隅操作影响。
- 新增可排序播放队列、音量、停止/列表循环/单曲循环、全局快捷键和跨页面持续播放。
- 新增全局、作品、章节、专注四层播放上下文；按具体程度回退，自动切换默认关闭。
- 新增写作侧栏伙伴、名称/静态表情和本地离线 Mock 对话；自定义 provider 在未绑定明确协议前不会发送内容。
- 新增伙伴权限中心，逐项授权作品、章节、资料数量和音乐上下文；加密作品需要仅本次解锁会话有效的额外许可。
- 同版本补丁统一智能创作设置区的字号、表单和层级，明确自定义 Provider 仅做配置检查；提升伙伴侧栏可读性，并补齐窄窗口伙伴抽屉与音乐响应式布局。
- 同版本补丁扩展正文工具栏：增加正文/三级标题、文字高亮、四种对齐、待办列表、链接编辑与移除、清除格式、列表缩进和查找替换，并按操作语义重新分组；根据体验反馈移除代码类工具入口。
- 同版本补丁让正文宽度随编辑区域扩展、工具栏按容器宽度折叠，并让当前章节的删除/回收站入口保持可见；GPT Image 2 全局背景图的生成数量、尺寸和统一风格提示词模板最终收拢为帮助中心的常用指南小项。
- 后续反馈补丁取消正文 1120px 上限并移除编辑区左右分隔线；音乐空提示只在音乐页显示且可关闭；背景图说明合并为常用指南折叠小项。
- 后续反馈补丁为正文段落和标题加入默认、1.25、1.5、1.75、2.0 五档可持久化行间距。

## 1.0.0 · M6 首个长期可用版

- 新增三步首次启动向导，可在创建资料库前设置资料库目录、备份目录和默认主题，也可接入已有一隅资料库。
- 新增内置“帮助中心”，集中说明自动保存、字数、备份、加密、导入导出和忘记密码；同版本补丁取消固定十项场景与 7 天试用记录。
- 同版本补丁新增正文选词资料标记：可关联或直接创建人物、地点、事件，并形成章节反向引用。
- AI 印象图发送前默认将已标记资料替换为稳定的匿名占位符，并显示实际发送预览与替换数量。
- 同版本补丁统一人物、地点、事件、作品、卷、章节和素材的删除确认 UI，展示引用与恢复影响，并区分回收站和永久删除。
- 新增脱敏诊断包和 Rust 崩溃标记；只输出版本、平台、数量、大小和代码位置，不包含正文、标题、完整路径或 API Key。
- 资料库文件、图片、恢复草稿、vault 和备份统一跟随可配置资料库根目录；秘密仍保留在 Windows DPAPI 保护的应用目录。
- 窗口失焦、隐藏、超时或退出时重新锁定已解锁作品，并补充 10,000 条资料搜索、10 万字、升级兼容、磁盘失败和隐私回归基线。

## 0.5.0 · M4–M5 图片与数据安全闭环

- 图片素材、正文图片、AI 印象图、完整备份恢复、开放格式导入导出和作品级加密。
- 同版本补丁把插图入口移入正文工具栏，并将设置页整理为可记忆的折叠分类。

## 0.2.0 · M3 资料与时间线

- 人物、地点、事件、自定义字段、模糊时间、章节锚点、双向引用、写作资料侧栏、时间线排序和资料回收站。

## 0.1.0 · M0–M2 写作核心

- 可安装应用外壳、作品/卷/章节、富文本、可靠保存、异常草稿、版本历史、搜索、专注模式和持久化设置。

## 0.0.1 · 首个原型

- 首个可运行桌面原型与基础写作界面。
