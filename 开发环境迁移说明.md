# 一隅开发环境迁移说明

本说明用于把当前完整源码工作区迁移到另一台 Windows 电脑继续开发。迁移 ZIP 保留源码、文档、`.agent/` 状态、`.github/`、原始需求材料和当前 `.git/` 目录；不包含可重建的依赖与编译缓存。

## 1. 迁移包不包含什么

- `node_modules/`、`.pnpm-store/`：由 `pnpm-lock.yaml` 重建。
- `apps/desktop/dist/`：由 Vite 重新构建。
- `apps/desktop/src-tauri/target/`：Rust/Tauri 编译缓存和安装包，当前约 13.2 GB。
- `.env`、`.env.local`、编辑器缓存和日志：避免迁移机器相关配置或秘密。

这不会删除旧电脑上的任何文件。迁移 ZIP 也不包含一隅应用正在使用的个人资料库、加密作品密码或 DPAPI 保存的 Provider 密钥。若还要迁移实际写作数据，请在旧电脑的一隅中额外创建 `.yiyu-backup` 完整备份；API Key 需要在新电脑重新填写。

## 2. 新电脑需要安装

1. Windows 10/11 与 Microsoft Edge WebView2 Runtime。
2. Node.js 24；当前验证环境为 `v24.18.0`。
3. Corepack / pnpm；项目固定 `pnpm@11.20.0`。
4. Rustup 与 `stable-x86_64-pc-windows-msvc`；当前验证环境为 Rust/Cargo `1.97.1`。
5. Visual Studio Build Tools，勾选“使用 C++ 的桌面开发”和 Windows SDK。

## 3. 解压与恢复依赖

建议把 ZIP 解压到不含同步盘、不过深的目录，例如 `D:\Projects\yiyu`。然后在 PowerShell 中执行：

```powershell
cd D:\Projects\yiyu
corepack enable
pnpm --version
pnpm install --frozen-lockfile

rustup default stable-msvc
rustup update
rustc --version
cargo --version
```

如果 Corepack 尚未下载项目固定版本的 pnpm，首次执行可能询问是否从 npm registry 下载，选择 `Y`。

## 4. 接手前验证

```powershell
pnpm typecheck
pnpm lint
pnpm test

cd apps\desktop\src-tauri
cargo test
cd ..\..\..

pnpm tauri:dev
```

需要生成 Windows 安装包时执行：

```powershell
pnpm tauri:build
```

## 5. Git 与 Agent 状态

当前 `.git/` 目录没有任何提交，项目文件在 Git 中仍全部处于未跟踪状态。迁移包保留了这个事实，但它不能替代一份正式版本历史。解压并验证成功后，建议立即检查并创建首次提交：

```powershell
git status
git add .
git commit -m "chore: preserve yiyu 2.0.0 migration baseline"
```

继续开发前按根目录 `AGENTS.md` 读取 `.agent/PROJECT_STATE.md`、`.agent/ROADMAP.md`、`.agent/QUALITY_GATES.md` 和 `.agent/SYSTEM_AUDIT.md`。当前产品版本为 `2.0.0`，M8 状态为“已交付待自然体验”；不要从 M0 重新实现。

## 6. 迁移完成检查

- `pnpm install --frozen-lockfile` 成功，锁文件没有被意外改写。
- `pnpm typecheck`、`pnpm lint`、`pnpm test` 和 `cargo test` 通过。
- `pnpm tauri:dev` 能打开主窗口，桌面伙伴可正常显示和随主窗口退出。
- `.agent/`、`docs/`、`AGENTS.md` 和中文任务说明文件均存在。
- 如需个人资料，在应用内恢复单独生成的 `.yiyu-backup`，并重新配置 Provider API Key。
