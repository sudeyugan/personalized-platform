# Agent Project Guidance

## Purpose

本项目使用 `.agent/` 中的文件维护目标、状态、计划、质量标准、决策、风险和经验。Agent 必须主动维护这些信息，不能等待用户反复提醒。

## System heartbeat — always apply

1. 新会话、跨电脑、上下文不确定或恢复长期任务时，读取本文件和 `.agent/PROJECT_STATE.md` 的当前状态部分。
2. 只有里程碑/发布、流程或架构变化、重大偏差、连续失败，或状态可能失真时，才完整读取并更新 `.agent/SYSTEM_AUDIT.md`。
3. 普通局部修复不重复通读历史检查点、历史门禁证据或产品基线；按当前任务读取相关文件即可。
4. 结束任务时，只有里程碑、验证状态、计划、风险、决策或用户验收发生实质变化才更新状态；纯调查和普通小改不制造无信息量检查点。

## Required reading order

首次进入项目、跨机迁移或无法确认基线时：

1. `.agent/PROJECT_BOOTSTRAP.md`
2. `.agent/PROJECT_BRIEF.md`
3. `.agent/PROJECT_STATE.md`
4. `.agent/ROADMAP.md`
5. `.agent/QUALITY_GATES.md`
6. `.agent/CODE_STRUCTURE.md`
7. `.agent/SYSTEM_AUDIT.md`
8. 与任务相关的 `DECISIONS.md`、`RISKS.md` 和 `LEARNINGS.md`

普通任务恢复只需重新读取本文件、`PROJECT_STATE.md` 当前摘要及与改动直接相关的规则。`docs/archive/` 和各文件历史章节仅在追溯事实时读取，不属于日常必读。代码、Git 状态和相关测试仍需用于验证当前事实。

## Communication contract

- 开始工作时先复述目标、范围和验收标准。
- 开始版本开发或处理体验问题前，读取 `docs/体验反馈与需求补充.md`；正式版本新增章节，同版本补丁只更新原章节中的问题或补充项。保留用户原始反馈，只标记处理结果。
- 默认采用“完整阶段交付”协作方式：进入一个 Roadmap 里程碑后持续实现到该阶段完成条件和可自动执行的验收全部通过，再集中向用户交付和收集反馈。内部检查点只更新 `.agent/` 状态，不作为版本交付，不要求用户逐个确认。
- 仅在目标或范围需要用户决策、出现真正阻塞、需要新增权限/成本，或剩余验收客观上必须由用户人工操作时，才在阶段完成前暂停交互；人工验收应尽量集中到阶段末一次提出。
- 主动澄清会改变产品方向、范围、兼容性、架构或验收结果的歧义。
- 可以从代码和配置查明的技术事实由 Agent 自行调查，不机械询问用户。
- 重要假设必须明确标注；不把推测当成已确认需求。
- 发现实际情况会导致明显偏离时，先告知用户，再扩大改动。
- 长任务应提供简短、可验证的阶段更新，让用户能够及时纠偏。

## Automatic checkpoint protocol

以下事件发生时，必须更新 `.agent/PROJECT_STATE.md`：

- 完成一个可验证里程碑。
- 验收测试首次通过或从通过变为失败。
- 当前实现与目标基线出现差异。
- 计划、优先级、架构、范围或验收条件发生变化。
- 出现新的阻塞、风险或待用户决策。
- 长时间工作但尚未形成里程碑。
- 任务暂停、结束、切换 Agent 或准备交接。

检查点必须回答：

1. 已完成什么，证据是什么？
2. 正在做什么？
3. 下一步是什么？
4. 与原始目标有何偏差？
5. 偏差属于实现差异、优化候选、缺陷、技术债、风险还是范围变化？
6. 是否需要调整计划，如何调整？
7. 哪些事项需要用户确认？

不要在每次微小编辑、定向测试通过或普通反馈修复后更新状态；以可验证里程碑、发布状态和真实决策点为单位。

## Periodic system audit

按 `.agent/SYSTEM_AUDIT.md` 执行审查，但只在以下事件触发：

- 开始、完成或交接里程碑/正式发布。
- 上下文压缩后无法确认目标或当前状态。
- 计划、架构、范围、验收标准或高风险边界发生变化。
- 连续失败使当前计划可能失真，或用户指出明显遗漏/偏离。

普通问答、只读调查、文案/CSS 小修和已覆盖的定向测试不触发完整系统审查。约 30–45 分钟只作为长任务中的状态失真提示，不是机械计时打卡。

## Baseline protection

`.agent/PROJECT_BRIEF.md` 是目标基线。未经用户确认，不得修改目标、范围、非目标或验收标准。

当需求变化时：

1. 在 `PROJECT_STATE.md` 记录偏差和影响。
2. 与用户确认是否接受变化。
3. 在 `DECISIONS.md` 记录决定。
4. 获得确认后再更新 `PROJECT_BRIEF.md` 和 `ROADMAP.md`。

## Evidence rules

- “已完成”必须有与风险相称的证据；普通低风险任务可以依据代码差异、静态检查或用户体验，不强制自动测试。未运行测试时必须明确说明，不把“未测”写成“已验证”。
- “优化”必须有性能、可靠性、复杂度、体验或维护性方面的证据。
- 未满足原验收标准的实现不得称为优化。
- 不使用没有依据的精确完成百分比；使用里程碑和验收项表示进度。
- 区分既有失败和本次修改造成的新失败。

## Work loop

```text
确认目标 → 调查事实 → 计划最小增量 → 实现 → 验证 → 检查偏差 → 更新检查点
```

- 优先解决根因。
- 保持改动聚焦，不顺手重构无关区域。
- 保护用户已有未提交修改。
- 新依赖、外部写入、生产部署或高权限操作遵守授权边界。
- 使用 `.agent/QUALITY_GATES.md` 决定验证强度。
- 普通开发默认优先 coding，不自动运行测试或完整 verify。UI、文案、布局和体验问题可由用户实际使用发现；只有复杂逻辑、易复发缺陷或用户要求时才运行少量 `verify:quick`。`verify:ui` 只做 typecheck、lint 和 Web build，不跑测试；Rust、Cargo/Tauri、存储、备份、加密、秘密、权限或范围不明时使用 `verify:full`。只有用户明确需要安装包、正式补丁或里程碑发布时才运行 Tauri/NSIS。
- 当前用户已明确设定持续偏好：每次代码修改完成后自动构建 Windows 安装包并放到桌面，除非用户当次明确说不用；纯文档修改不触发安装包构建。该偏好只增加构建与交付动作，不自动扩大 lint、测试或正式发布门禁。
- 修改文件时按 `.agent/CODE_STRUCTURE.md` 检查长度、职责、复杂度和模块边界。
- 重大技术或产品决策追加到 `.agent/DECISIONS.md`。
- 新风险写入 `.agent/RISKS.md`。
- 可跨任务复用的经验写入 `.agent/LEARNINGS.md`。
- 按触发条件执行 `.agent/SYSTEM_AUDIT.md`，不要依赖用户提醒。

## Code structure guard

- 文件达到 `CODE_STRUCTURE.md` 的关注阈值时，必须做结构评估，但不得仅为降低行数机械拆分。
- 文件超过重构阈值，或同时承担多个可独立变化的职责时，应在继续堆叠功能前提出并执行适当级别的拆分。
- 优先按业务职责和变化原因拆分，而不是按任意行数切块。
- 未来可能继续深入发展的功能，应升级为独立目录模块，并保持清晰的公共入口。
- 重构必须尽量保持行为不变，并在前后运行相关质量门禁。
- 大范围架构重构、公共 API 变化或可能影响当前交付节奏的拆分，先与用户沟通。

## Completion

完成声明必须有与风险相称的证据，但不要求每个小任务都形成一次“发布”。

- 普通任务：说明完成内容、实际运行过的检查和残余风险；默认允许不跑自动测试并交给用户体验，无状态变化时不更新治理文档。
- 批次验证：按需运行 `verify:ui`；只有跨层高风险或正式大版本运行 `verify:full`。两者都不生成安装包、不刷新发布哈希。
- 正式补丁/里程碑：运行对应 release 命令，才同步版本状态、CHANGELOG/反馈、必要的 README/产品注记、安装包和哈希。
- 只更新事实确实变化的文档。架构决定写 `DECISIONS.md`，新增持续风险写 `RISKS.md`，可复用流程经验写 `LEARNINGS.md`；不得为了满足清单重复抄写同一证据。
- 自动生成区块只修改 `.agent/project-status.json` 后运行 `pnpm docs:sync`；正式交付通过 `pnpm docs:check` 和 `pnpm release:check`。
