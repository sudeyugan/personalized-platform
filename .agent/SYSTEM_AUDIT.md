# Agent Development System Audit

> 本文件只保存当前审查规则和近期重要记录。旧记录见 [`docs/archive/SYSTEM_AUDIT_HISTORY.md`](../docs/archive/SYSTEM_AUDIT_HISTORY.md)。

最后审查：2026-09-14（质量门禁与文档流程精简）  
审查结果：合规  
下一次触发：里程碑/正式发布、重大流程或架构变化、状态失真、连续失败或上下文无法恢复

## 触发条件

仅在以下情况执行完整审查：

1. 开始、完成或交接里程碑/正式发布。
2. 计划、架构、范围、验收标准、安全边界或持续风险变化。
3. 上下文压缩后无法确认目标和当前事实。
4. 连续失败、用户指出偏离，或状态文件可能失真。

普通问答、只读调查、文案/CSS 小修、定向测试通过和没有状态变化的任务不触发完整审查。时间间隔只提示长任务检查是否失真，不作为机械打卡。

## 审查要点

- 读取 `PROJECT_BRIEF.md`、`PROJECT_STATE.md` 当前部分及与任务直接相关的规则。
- 用代码、Git 和风险匹配的验证校验状态，不通读归档历史。
- 检查目标/范围/兼容性/安全边界是否变化，以及是否需要用户确认。
- 只更新事实发生变化的状态、计划、决定、风险或经验文档。
- 正式发布确认验证、安装包、哈希、文档与人工状态相互一致。

## 审查记录

### 2026-09-14：质量门禁与文档流程精简结束审查

- 触发原因：用户确认降低每个版本和小补丁的重复 gates、时间与 token 成本。
- 审查结果：合规。验证强度继续由风险决定，但开发验证、候选打包和正式发布已解耦。
- 完成：新增 quick/ui/full 验证与独立候选打包命令；普通任务不再自动生成 NSIS、刷新发布状态或同步全套文档；历史流水移至 `docs/archive/`。
- 验证证据：门禁脚本测试 11/11；quick 仅运行指定文件 2/2、约 1.14 秒；UI 门禁通过 TypeScript、oxlint、前端 69/69 和 Web build，未运行 Rust/Tauri/NSIS；文档一致性通过。
- 安全边界：普通 UI 可不测试；Rust、Cargo/Tauri、存储、备份、恢复、加密、秘密、权限或范围不明仍强制 full。UI 正式补丁省略前端/Rust 测试，正式大版本和跨层补丁不降级。
- 偏差：无产品或架构偏差。用户授权 Agent 管理 Git；首次基线使用 GitHub noreply 仓库本地身份并推送至 `sudeyugan/personalized-platform`。门禁范围仍由明确风险判断，不自动降级。
- 下一步：使用新分级命令开发；只有明确交付安装包时运行正式 release。首次 Git 基线由用户另行决定。

### 2026-09-13：新电脑开发环境迁移结束审查

- 触发原因：迁移包已复制到新电脑，用户要求恢复到 `D:\coding\yiyu`，任务进入验证与交接阶段。
- 审查结果：合规。产品版本仍为 `2.0.0`、M8 仍为已交付待自然体验；未改变产品基线、功能范围、依赖锁定或后续 Roadmap。
- 完成：ZIP 外层校验一致；1509 个归档文件解压后逐文件 SHA-256 一致；Node、pnpm、Rust/Cargo、MSVC 与 WebView2 开发前置已就绪，依赖按冻结锁文件恢复。
- 验证：TypeScript、oxlint、生产 Web 构建和文档一致性通过；前端 69/69、Rust 26/26 通过。未启动 GUI，桌面窗口和真实角色包仍保留为自然体验项。
- Git 与安全：仓库原本没有提交且源码全部未跟踪，该事实保持不变；因归档文件所有者不同，仅为精确路径增加 Git `safe.directory`，没有扩大到其他仓库，也未擅自创建首次提交。
- 偏差与残余：无产品或架构偏差；迁移新增 `node_modules`、`dist` 和 Rust `target` 等可重建本机缓存。个人资料库、密码和 DPAPI Provider 密钥仍需通过应用备份或重新配置，未进入源码目录。
- 下一步：用户自然运行 `pnpm tauri:dev`；确认 Git 用户身份后建立首次基线提交。下一次触发为首次提交、桌面体验反馈、上下文恢复或新开发任务开始。

### 2026-09-13：跨电脑开发迁移包结束审查

- 触发原因：用户要求把完整项目打包并迁移到另一台电脑继续开发，任务进入交接和结束阶段。
- 审查结果：合规。产品版本仍为 `2.0.0`、M8 仍为已交付待自然体验；未修改目标基线、功能代码、依赖版本或发布产物。
- 完成：新增开发环境迁移说明并从 README 链接；迁移包保留源码、锁文件、治理/产品文档、原始输入和当前 Git 元数据，排除约 13.2 GB Rust 构建缓存及其他可重建/机器相关目录。
- 验证：文档生成与一致性检查通过；最终 ZIP 实际解压后与源目录逐文件比较 1509 个文件，缺失、额外、长度/哈希不一致均为 0，外层 SHA-256 另存同目录校验文件。
- 风险与边界：当前仓库没有任何提交，不能用 Git 历史恢复未跟踪源码；源码包也不包含应用个人资料库、DPAPI 密钥或用户密码。迁移说明要求个人数据另做 `.yiyu-backup`，新电脑重新配置 API Key。
- 偏差：无产品或架构偏差；不把 13.2 GB `target` 与 201 MB `node_modules` 放入迁移包属于可重建产物排除，不构成项目内容缺失。
- 下一步：新电脑解压后按迁移说明恢复依赖、执行前端/Rust 门禁和 `pnpm tauri:dev`；验证成功后立即建立首次 Git 提交。

### 2026-09-15：角色 Slot 架构兼容审查

- 触发原因：角色差分从仅完整画布扩展为局部 Sprite + Slot，属于角色渲染数据结构的兼容演进。
- 审查结果：合规。旧字符串资源继续以原点全画布渲染；新资源坐标只由 Renderer 根据角色局部 Slot 解析，高层 Controller 不感知坐标。
- 安全边界：未增加依赖、网络、文件读取或桌面窗口权限；仍只读取主窗口已注册的当前角色包素材。
- 验证边界：TypeScript 和两个定向角色用例已通过；遵照用户减少自动验证的要求，未运行完整 lint、全量 test、Web/Tauri build 或安装包构建。
- 残余风险：实际 AI 素材的内部留白、风格和动作连续性仍需在开发 Slot 边界与自然使用中观察；缺失 Slot 会记录明确错误并回退到角色原点，不导致运行时崩溃。

### 2026-09-15：M9 日常规划范围审查

- 触发原因：用户暂停虚拟形象素材路线并把产品范围扩展到日记、课表和待办，属于明确的范围与阶段变化。
- 审查结果：合规。规划功能保持本地优先，不接外部教务系统；后续仅按用户明确要求导入截图中的课程，不提取姓名、学号等个人信息。旧写作、音乐和伙伴功能保持不变。
- 数据与兼容：PlannerData 随既有 LibraryData 保存、备份和恢复；旧资料库通过 normalizeLibrary 补空数据。没有新增依赖、网络权限、Rust command 或文件访问能力。
- 验证边界：本轮未运行 lint 或测试；随后用户明确要求本地编译，TypeScript、生产 Web、Rust Release 与 NSIS 打包成功。M9 仍标记为进行中而非正式发布或已交付。
- 残余风险：课表仍固定为截图所示七天六节时间；已支持 16 周学期以及全周、区间、单双周解析，但尚未提供自定义作息、学期设置界面或课程冲突提示，待实际使用反馈后再选择增量。

### 2026-09-15：M9 需求纠正审查

- 纠正：功能主语义改为“日历与课表”；日记后来进一步拆到创作空间成为独立单栏页面，日历只保留日期索引与跳转。截图课表经用户明确要求进入资料库，不再保持空数据。
- 数据兼容：稳定课程 ID 和 courseImportVersion 防止重复迁移；已有手工课程按星期、节次和标题保留并去重。重复待办旧数据补 repeat=none 与空 completedDates。
- 范围控制：重复方式最终收敛为不重复、每日和每周自选星期；旧工作日映射为周一至周五。完成率从既有 completedDates 实时派生，不引入项目、标签、子任务、提醒服务或额外统计存储。
- 验证：TypeScript、生产 Web、Rust Release 和 NSIS 本地构建成功；未运行 lint 或测试，继续以自然体验为主要反馈方式。

### 2026-09-15：日历与日记职责拆分审查

- 决定：日历/课表继续属于日常规划，日记独立归入创作空间；两者只通过所选日期和 diaryEntries 关联。
- 兼容：旧 journal 视图与导航顺序归一化为 calendar，已有日记内容不改写；日记随写作模块显隐。
- 结构：拆成 CalendarScheduleView、DiaryView 与共享 scheduleConstants，避免时间管理和正文编辑继续堆叠在单组件中。
- 验证：TypeScript 检查通过；未运行 lint、测试、Rust 或安装包构建。

### 2026-09-16：M9 候选状态同步审查

- 当前边界：月历只承载日程事务与日记标记；课程和待办在日期侧栏；日记为创作空间单栏编辑器；重复待办显示创建日至今的有效计划次数、完成次数和完成率。
- 数据与兼容：calendarEvents、term、repeatDays 随 PlannerData 保存；旧工作日和每周任务在 normalizeLibrary 中转换，统计不新增持久化字段。课程按 2026-09-14 起共 16 周及各课程周次过滤。
- 验证事实：M9 相关 TypeScript 检查、前端生产构建、Rust Release 和 NSIS 本地候选构建成功；智能创作 UI 候选包哈希更新为 `86A84D36DB603C25652F30A3E52F0BA715D12FB960B67D8FB4B809723C1B6FF0`。未运行 lint 或测试，不升级为正式交付状态。
- 流程：用户要求后续每次代码修改后自动编译并把安装包放到桌面；纯文档任务除外，该偏好不等价于运行完整发布门禁。
