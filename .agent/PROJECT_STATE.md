# Project State

<!-- generated:project-status:start -->
最后更新：2026-09-21  
当前版本：`2.0.0`  
当前阶段：M10 AI 伙伴 Agent 与立绘基础  
阶段状态：进行中  
下一阶段：后续方向待用户确认  
自动门禁：已通过  
人工验收：待验收
<!-- generated:project-status:end -->

## 状态摘要
- 2026-09-21：WebM 聊天开关继续补丁。指针层修复后实机已能打开但不能关闭，进一步定位为透明聊天窗显示成功、聚焦失败时 `chatVisible` 缓存未置真。切换现以 Tauri `isVisible()` 为事实来源，显示后先记录状态再尝试聚焦，并加互斥避免快速重复切换。TypeScript、lint（保留既有非阻断警告）、前端生产构建、Rust Release 与 NSIS 已通过；桌面直接版已更新，EXE SHA-256 为 `C327C6AF93A775ABFDC2C4D256EDFBD6370A8C6B146F5C8BB220BCA51EF6FEE7`，安装包 SHA-256 为 `DC6811BC611188B9032DAB49E9673669F5F9868AE609C520A7E82F7FDD26D816`。
- 2026-09-21：修复启用透明 WebM 后桌面伙伴无法稳定开关聊天。媒体元素统一为 `pointer-events:none` 的纯显示层，新增覆盖整个立绘窗口的独立透明交互层并捕获活动 pointer，避免视频元素吞掉 pointerup；静态图与 WebM 现共享单击、拖动、双击和键盘逻辑。TypeScript、lint（保留既有非阻断警告）、前端生产构建、Rust Release 与 NSIS 已通过；桌面直接版已更新，EXE SHA-256 为 `D7EC6149C1DEB6A036455823CDDC4A285FF7C88C6990CD05F959B9C9AF7D864D`，安装包 SHA-256 为 `CDA5DA45459CE269F35CAA0C85411885953FC8AF12196B3D0A697F64113BF04E`。
- 2026-09-21：伙伴输出过程新增可恢复交互。桌面聊天可暂停/继续或停止朗读，并能中止当前 Agent 回合；主界面生成期发送键切换为中止键，输入框仍可预写下一条。AbortSignal 贯穿 UI、Conversation、Runtime 与 Provider；中止后不再执行 Tool、写入确认或保存回答，迟到 SSE 按回合隔离。TypeScript、Agent/Provider 定向测试 18/18、前端生产构建、Rust Release 与 NSIS 已通过；桌面直接版已更新，EXE SHA-256 为 `D285FC908BA9C1D0975CD765ABDB73238479EA4972CA17C818449DE27E291411`，安装包 SHA-256 为 `3521C84E16B6387F6CD3A2D8304A133FD3AA7861AD338CCF01C058239D1D3D71`。
- 2026-09-21：M10 从只读 Agent 演进为受控读写闭环。新增默认关闭的写入能力、逐次确认 UI、scope 先于确认的权限顺序，以及待办/日历/日记/章节/记忆写入和页面/音乐控制；删除、密钥、文件、Shell 与系统操作仍拒绝。语音提问支持精简/标准长度，长回答默认只朗读前段并可停止，消息安全渲染 Markdown。TypeScript、Agent/Provider 定向测试 17/17、lint（仅保留既有/非阻断警告）、前端生产构建、Rust Release 与 NSIS 已通过；桌面直接运行版已更新，EXE SHA-256 为 `C08FD509E6987E3A726C95835078AAC3B28C09A8334EC6FCB769D734B19FDA0A`，安装包 SHA-256 为 `2B40DBC2094AD899FD565A5E96A588081777E9CA6A3BB50CD7C1D4EBE357BC6F`。

- 2026-09-21：M10 对话升级为全链路增量体验：DeepSeek 由 Rust 解析 SSE，主界面和桌面聊天逐段显示；针对 `deepseek-flash` 当前协议，伙伴对话显式使用非思考模式并移除不兼容的 `tool_choice`，Tauri 字符串错误可原样反馈。ElevenLabs Scribe Realtime 使用一次性 token 返回 partial/committed transcript；自动朗读按完整语句在回答生成期间排队播放。只读 Tool 新增地点与时间线搜索，长期记忆因缺少独立外部模型授权仍不作为 Tool。
- 2026-09-20：M10 新增透明 WebM 动态立绘、桌面轻量聊天、真实 DeepSeek 对话/Tool Calling 与首页“朝问”；桌面聊天现拆为固定尺寸立绘窗口与独立透明聊天窗口，由主窗口按显示器空间左右定位，彻底取消打开聊天时对立绘窗口的移动/缩放。语音已通过独立 Provider 接口接入 ElevenLabs STT/TTS，默认关闭，Key 经 DPAPI 保存且 Rust 仅访问官方域名。

- M9 日常记录与个人仪式已交付待自然体验；当前 M10 已实现第一阶段只读 Agent Runtime、单张无边框立绘、可配置全局显隐快捷键，以及统一“AI 伙伴”设置中的 DeepSeek 对话/OpenRouter 图像分离配置。模型设置已改为上下排列，立绘只保留本地上传，不再提供 AI 草稿生成。
- 2026-09-19 新增日期级待办休假：当天今日列表清空、打卡入口禁用，休假日从重复任务应完成次数/完成率排除，并同步影响日历侧栏；旧资料自动补空休假记录，既有完成数据不删除。
- 最新本地候选安装包于 2026-09-21 构建，SHA-256 为 `A3FF5A946D8AB64D5891C0038552864C1FFA2AF7704CCB18D9F1B5950F7294DD`；Release 与桌面 `一隅_直接运行.exe` SHA-256 均为 `66F2A0CC9AF9E41FB99FDAF0CAE7A7FE6083FAA3B46F616CD76E1470040CD2D9`。DeepSeek 流式兼容补丁已通过 TypeScript、Rust check 与伙伴定向测试 14/14；ElevenLabs 令牌 411 修复通过 Rust 定向测试 2/2；弹窗输入字号补丁通过前端生产构建、Rust Release 与 NSIS。真实账号下的对话、实时麦克风和 Tool 查询待用户在新版确认。
- M7 `1.2.0` 已由用户继续 M8 的指令自然验收；M8 `2.0.0` 的桌面角色 Runtime 与退出生命周期同版本反馈补丁已交付待自然体验。
- 音频副本由 Rust 可信仓储管理并进入完整备份；支持有序队列、音量、三种循环、快捷键、跨页面播放和默认关闭的自动上下文切换。
- 伙伴默认无权读取资料；加密作品在持久作品/章节授权之外还需仅本次解锁会话有效的内存许可，锁定或撤销立即失效。
- 本地 Mock 不联网；自定义 provider 未选择兼容协议时明确拒绝发送。模型只通过声明 schema/capability/risk/scope 的 Tool Registry 查询现有 Application Service，桌面伙伴只读取主窗口注册的当前立绘素材白名单。
- M8 历史门禁：正式补丁前端 69/69、Rust 26/26、TypeScript、无警告 oxlint、生产双窗口 Tauri/NSIS、文档与哈希通过；不得误记为 M9 的验证结果。

## 本阶段完成

- [x] 本地音乐：白名单复制入库、元数据、回收站、永久删除与备份恢复。
- [x] 播放器：有序队列、音量、停止/列表循环/单曲循环、快捷键与跨页面持续播放。
- [x] 上下文：全局、作品、章节、专注四层回退；手动队列准确，自动切换默认关闭。
- [x] 伙伴侧栏：名称、静态表情、最近对话、本地 Mock 和可替换 provider 安全边界。
- [x] 权限中心：作品、章节、资料和音乐逐项授权；加密作品额外临时许可与撤销/锁定清理。
- [x] 兼容与发布：旧资料迁移、音频完整备份、失败路径、生产构建、NSIS 和 SHA-256 校验。

## 待人工验收

- [ ] 运行最新桌面直接版，确认导入透明立绘后无边框，Ctrl+Alt+Y 可在其他应用前台时显示/隐藏伙伴，修改或关闭快捷键后立即生效。
- [ ] 自然体验“AI 伙伴”的模型与生成、形象与陪伴、权限与记录三个分区；确认 DeepSeek 与 OpenRouter Key 文案、配置边界和窄窗口布局清晰。
- [ ] 上传真实 1792×3184 透明 WebM，确认透明通道、循环流畅度、状态回退、单击聊天/拖动/双击不会互相冲突；验证弹窗展开不移动立绘、靠近左右边缘时会换向、拖出屏幕后自动收回。
- [ ] 使用已保存的 DeepSeek Key 验证 SSE 逐段回复与人物/章节/地点/时间线查询；选择 ElevenLabs、保存独立 Key 与 Voice ID 后，确认录音时 partial transcript 持续进入草稿且自动朗读在回答完成前开始。
- [ ] 自然体验 M9 的日历事务、课表周次、日记单栏、每日心情、重复待办统计与答案之书；发现具体问题再定向修复，不设置逐项打卡门禁。

## 实现差异与技术债

| 项目 | 当前实际 | 等级 | 处理计划 |
|---|---|---:|---|
| SQLite 权威模型 | 资料、素材与 AI 表为可查询投影，权威写入仍是版本化 `library_state` 快照 | 1 | 1.0 保持兼容；无性能或一致性证据前不做破坏性逐实体改写 |
| i18n | 导航和资料共用文案已集中，但设置、首次向导、帮助及 M7 写作工具栏/音乐/伙伴仍有组件内中文 | 2 | 不影响简中交付；M8 扩大伙伴 UI 或工具栏继续增长前按 writing/companion/music 领域提取，禁止无边界扩张 |
| 文档转换 | 基础中文标题、正文、列表、引用和图片可迁移；复杂浮动图、分页与字体不能保证往返一致 | 2 | UI/发布说明明示限制，Markdown＋图片为长期开放兜底 |
| Windows 会话事件 | 已覆盖超时、窗口隐藏、失焦和退出；没有自动化真实锁屏/休眠驱动 | 2 | 不设固定人工矩阵；真实使用中发现问题时再做针对性回归 |
| 自动桌面 E2E | M7 音频由组件状态、Rust 文件/备份和生产构建覆盖，未引入 Tauri driver | 2 | 当前收益不足以覆盖驱动维护；M8 第二窗口 capability/多屏行为前必须重新评估 |
| 资料库迁移位置 | 首次建库可选目录，但已有资料库不能直接原地搬迁 | 2 | 通过完整备份在新空资料库恢复；发布说明明确限制 |
| 自定义伙伴 provider | DeepSeek 官方协议已接入；任意自定义端点仍保持安全失败 | 2 | 每个新服务单独决定协议、密钥槽和外发范围，不把 DeepSeek Key 发送到自定义地址 |
| 透明 WebM 兼容 | WebView2 对 VP9 alpha 与 1792×3184 循环视频的设备表现尚未由真实素材验证 | 2 | 用用户素材自然验证；失败时保留静态立绘/待机回退，再根据样本决定转码规范 |
| 音频编码兼容 | 白名单格式仍可能包含 WebView2 无法解码的特殊编码或损坏文件 | 2 | 失败不影响写作；根据真实文件样本补解码提示或转码方案 |

## 下一步

1. 运行桌面的 `一隅_直接运行.exe`，上传真实透明 WebM，验证静态/动态切换、单击聊天、拖动、双击和 DeepSeek 查询。
2. 验证首页“朝问”的生成、重试、写入日记与携带问题打开伙伴聊天；08:00 时应用未运行会在下次启动补生成。
3. M10 仍是进行中，不把本地候选构建记作正式发布；写入 Tool、确认 UI、Live2D、MCP 与 Computer Use 继续保持未实现。
4. 用户已要求以后每次代码修改后自动构建 Windows Release 并更新桌面直接运行版；纯文档同步不打包，除非用户另有要求。
5. 仍不建议把唯一一份重要原稿交给任何单一应用或设备。

## 最近检查点

### 2026-09-21：AI 伙伴流式文字、语音与只读能力扩展

- 模型：DeepSeek 请求启用 SSE；Rust 保持官方域名、密钥和载荷限制并解析事件，Tauri Channel 向 Model Provider 发送文本 delta 与分片 Tool Call。两个聊天界面显示临时增量回复，最终只持久化一条完整消息。针对 `deepseek-flash` 默认思考模式与 Tool Calling 的当前兼容约束，实时伙伴对话显式使用非思考模式且不再发送 `tool_choice`；Rust/Tauri 字符串错误会保留实际错误码。
- 语音：ElevenLabs STT 改用 Scribe v2 Realtime；主窗口以 DPAPI Key 获取 15 分钟单次 token，聊天窗口仅持临时 token，经官方 WebSocket 接收 partial/committed transcript。同源托管音频 worklet，CSP 只开放 ElevenLabs 官方 WSS。令牌 POST 显式发送零长度请求体和 `Content-Length: 0`，避免网关返回 411；HTML 网关错误不再原样展示。自动 TTS 按完整语句有序生成和播放，可在模型仍输出时开始。
- Agent：新增 `place.search` 与 `timeline.search`，继续复用 `records` 权限、参数 schema、ReadOnly 风险和审计。长期记忆未开放为 Tool，避免缺少独立外发授权时被发送给 DeepSeek。
- 验证与交付：原流式批次通过 TypeScript、Rust check、前端生产构建与伙伴定向测试；DeepSeek 兼容补丁通过定向测试 14/14，ElevenLabs 411 补丁通过 Rust 定向测试 2/2；弹窗输入字号补丁重新完成 Windows Release 和 NSIS。桌面 EXE 与 Release 哈希一致，为 `66F2A0CC9AF9E41FB99FDAF0CAE7A7FE6083FAA3B46F616CD76E1470040CD2D9`；安装包 SHA-256 为 `A3FF5A946D8AB64D5891C0038552864C1FFA2AF7704CCB18D9F1B5950F7294DD`。真实 DeepSeek 与 ElevenLabs 账号交互仍由用户在桌面新版自然验收。

### 2026-09-20：透明 WebM、桌面聊天、DeepSeek 与朝问

- 形象：支持用户上传 1792×3184 等 9:16 透明 WebM，按待机/倾听/思考/回应/轻松/关切/意外七个语义槽播放，缺失状态回退待机；静态 PNG/WebP 仍可切换且视频槽不会丢失。
- 交互：桌面立绘单击展开紧凑聊天卡，拖动移动，双击打开主程序；桌面窗口只发事件和读取主窗口登记的素材，Agent/权限/资料读取仍全部在主窗口。
- 模型：DeepSeek chat/tool calling 经 api.deepseek.com 白名单 Rust 代理运行，API Key 由 DPAPI 读取；任意自定义 Provider 不联网。新增低风险 `companion.set_state`，只允许选择受限视觉状态。
- 首页：删除“最近落笔”，新增“朝问”；北京时间 08:00 或下次启动生成，70% 平衡/30% 尖锐，保存 90 天主题避免重复，只有主动点击才写入日记。
- 本轮补丁：首页朝问改为问题/背景/追问双栏层级并删除人物、地点、往事概览；聊天卡缩小，展开保持立绘屏幕坐标，空间不足时改向另一侧，多显示器边界校正会把拖丢的窗口收回。
- 闪烁最终处理：连续实机反馈证明同一 WebView 向左移动原点并扩宽仍不可稳定消除闪帧，因此撤销该扩窗路径，新增 `companion-chat` 独立透明窗口。立绘窗口固定不动，主窗口只定位/显示聊天窗口；两个子窗口都不能直接访问资料、密钥或 Agent。聊天标题栏保留“新对话”，清空当前消息但保留长期记忆。TypeScript、Rust check 与 Windows Release/NSIS 通过，桌面直运行版已更新。
- WebM 导入与设置 UI 修复：真实下载样本为约 8.6 MB 的 VP9 WebM，旧实现却把全部字节转成 JSON 数组传给 Rust，造成选择后近似无响应。现使用 Tauri `InvokeBody::Raw` 直接发送 `Uint8Array`，文件名以 Base64 UTF-8 请求头传递；静态立绘与动态 WebM 采用二选一编辑页，动态页优先呈现必需的基础待机，其余状态默认折叠，并持续反馈读取信息、读取文件和本地保存阶段。元数据预读有 12 秒上限，失败时仍继续导入，避免编码预览阻断素材保存；只有基础待机存在时才启用动态显示，移除待机后自动回退静态立绘。TypeScript、Rust check、Windows Release/NSIS 通过，样本实际选择与透明播放待用户在新版自然验证。
- 显示链路补丁：桌面窗口读取视频也改用 Tauri 原始二进制响应，避免保存成功后再次把 WebM 序列化为数百万个 JSON 数字；素材 URL 在伙伴窗口会话内缓存。新静态图或视频先在不可见媒体层等待 `load/canplay`，就绪后才替换当前形象，初始化及切换空档不再回退内置默认人物；设置页接收实际显示成功或解码失败事件。快捷键选择器和静态/动态模式卡同步收敛视觉层级。TypeScript、Rust check、生产前端、Windows Release 与 NSIS 通过，未运行 lint 或测试。
- WebM 实机诊断：`D:\Download\1.webm` 为 1792×3184、4.033 秒、VP9、AlphaMode=1；Edge 153 无界面实测达到 `readyState=4` 且无媒体错误。资料库中两份保存副本均与原文件大小和 SHA-256 完全一致。应用内报错最终定位为 CSP 缺少 `media-src blob:`，现只允许同源与本地 Blob 媒体，不开放远程媒体域名；修复版生产前端、Rust Release 和 NSIS 构建成功。
- 语音：新增 ElevenLabs Scribe v2 转写、可配置 TTS Voice ID 与自动朗读；默认关闭且只由主窗口 Rust 固定访问官方域名，其他服务保留 Provider Adapter 扩展点。
- 验证：TypeScript 与 Rust cargo check 通过；伙伴/迁移定向测试 7 项通过；Windows Release 与 NSIS 成功。误触发的现有全量测试仍有 5 项旧 UI 断言失败（旧“智能创作”名称及折叠导航入口），与本轮运行逻辑无关。
- 验证与交付：TypeScript、20 项相关测试、Rust check、生产前端、Rust Release 和 NSIS 成功；rustfmt 未安装，因此未运行格式器，lint 和全量测试按本轮精简验证策略未运行。桌面 EXE 与 Release 源文件哈希一致，为 `437247B29690E552ACD83123AB79F14FDA44E25E405B6BB1F96CC24015249A8B`；安装包 SHA-256 为 `E9E684FA878B2E3159AB839820CB3BBB8F717B5E0626F9977A6B916F293EC633`。

### 2026-09-18：AI 伙伴单页设置与纯立绘桌面窗口

- 设置：移除模型、形象、权限三个互斥入口，合并为一个“AI 伙伴”页面；仅保留形象与显示、模型服务、权限与记录三条折叠行，默认打开常用形象设置，低频内容按需展开。
- 桌面：移除顶部拖动条、打开/关闭图标、伙伴名称、状态说明与全部底板；Tauri companion 窗口关闭系统 shadow，只渲染可拖动立绘。双击立绘仍可返回主窗口，隐藏使用全局快捷键或设置开关。
- 验证：TypeScript、lint、前端生产构建、Rust Release 与 NSIS 成功；lint 仍只有 4 条既有警告。最终同时移除程序级立绘投影；最新 EXE SHA-256 为 `DE50118AA70C948F0A9B80FDD2F7E76F0FE1D4406D882347DB23639F62EB9F67`，安装包为 `AD60B3C7022F3237232201199717F63847E0CBCD45808D0E61566F23E4C099C6`。
- 交付：桌面原文件短暂被 Windows 文件锁占用；未擅自终止进程，稍后重试已成功更新固定名称 `一隅_直接运行.exe` 并校验哈希，临时“新版”副本已删除。

### 2026-09-18：M10 AI 伙伴设置、无边框立绘与全局显隐快捷键

- 体验：桌面 portrait 与说明标签移除边框、卡片背景和按钮阴影，保留透明窗口及轻量文字阴影；默认 `Ctrl+Alt+Y` 可在系统范围切换伙伴显隐，设置提供三组替代组合或关闭，冲突时只提示失败。
- 设置：原“智能创作”统一命名为“AI 伙伴”，收敛为模型与生成、形象与陪伴、权限与记录三块。DeepSeek 对话默认地址/模型与 OpenRouter GPT Image 默认地址/模型分开配置，两套 Key 使用不同 DPAPI secret id。
- 安全：新增 Tauri 官方 `global-shortcut` 插件，只有主窗口 capability 可 register/unregister，回调只能切换伙伴可见性；在线 Provider 仍只检查配置，协议接入前不会发送内容。
- 验证与交付：前端定向 19/19、Rust 26/26、TypeScript、lint、生产 Web、Rust Release、NSIS 与文档一致性通过；lint 仅剩 4 条既有警告。桌面 EXE SHA-256 为 `9E2E8FCA9A951C44F0723A6E9822DDCEB546CB0230F28B539159548320F5F292`，安装包 SHA-256 为 `09A4395BF40949D73D6D4B9DBE975F2E5BF807AA899FF8F2BE975A81192355F4`。
- 状态：用户已授权把该范围登记为 M10；当前实现完成并等待用户自然验证，不标记为正式发布。

### 2026-09-18：伙伴只读 Agent 与单张立绘主路径

- Agent：把原来的一次性 `CompanionProvider.reply` 演进为可替换 `ModelProvider` 与最多 4 步的 Agent Loop；建立类型安全的 Context、Session、Tool Registry、Permission Engine、统一错误与脱敏 Audit。模型不持有 Store、Repository、SQLite、文件、Shell 或任意 Tauri command。
- Tool：真实只读入口为 `work.get_current`、`chapter.search`、`chapter.get`、`character.search`、`place.search`、`timeline.search`，统一调用受限 Application Service；执行前校验 JSON schema、Read capability、ReadOnly risk、资料 scope 与加密作品临时许可。长期记忆尚未获得单独外发授权，不作为 Tool。
- 立绘：桌面伙伴活动渲染改为一张透明 PNG/WebP，继续采用主窗口注册的最小素材白名单；旧 `characterPackage` 数据与解析代码仅保留兼容，不再提供导入入口或进入桌面快照。数据类型预留 `live2d`，未接入 Cubism SDK。
- 验证：Agent/Provider/伙伴定向测试 16/16、Rust 测试 26/26、TypeScript 检查通过；lint 退出码 0，剩余 4 条是旧角色 Renderer Fast Refresh 与待办 Hook 的既有警告。Release 构建成功，桌面直接运行版 SHA-256 为 `0358B44185F4D8E09A3B7685713D769E80447975A51C20CE8F74BDC57FFB1A50`，项目内 NSIS 安装包 SHA-256 为 `1B8EE9490C0033152244B2E8CCD0F93CFD82A4E405753181192A27E5A7C22737`。
- 边界：自定义在线 Provider 仍安全拒绝发送，当前 Mock 用于演示 Tool 闭环；写入 Tool/确认弹窗、完整 Session 持久化、Live2D、MCP 和 Windows 操作均未实现。误执行的一次全量前端测试为 78/83，5 个失败是现有 App/资料导航 UI 查询用例；本轮采用明确指定的 16 个相关测试作为验收证据。

### 2026-09-18：首页早中晚五点心情与融合色带

- 范围调整：心情记录从日历/日记移到首页唯一入口；日记恢复原单栏排版，日历只读回顾。每日固定早上、下午、晚上三个时段，每个时段把五枚点分配给 12 种情绪，同一时段重新保存即修改。
- 实现：编辑器同时保留五枚准确点与可选短备注，保存后按点数权重生成低饱和柔和渐变；首页显示当天三个时段、本周 7×3 色带、记录完整度、整体/分时段构成和高频情绪。日历格显示每日三条迷你色带，右侧展示当天明细及折叠月度构成。
- 配色调整：参考跨文化颜色—情绪研究，以较高明度承载积极感、较高饱和度提示高唤醒、冷色与灰度区分低唤醒和低落情绪；仍保持全套低至中饱和度，并为浅色情绪点改用深色文字，避免把颜色当作一对一心理诊断。
- 兼容与边界：缺失时段不计为零，不生成好坏均分、心理判断或 AI 分析。旧单一情绪记录按创建时间归入时段并转为五枚同色情绪点；同日同时段保留最新一条，继续进入现有本地资料库、备份和恢复链路。
- 验证与交付：TypeScript、前端生产构建、Rust Release 与 NSIS 成功；按用户偏好未运行 lint 或测试。桌面 `一隅_直接运行.exe` 已更新并与 Release 源文件哈希一致，SHA-256 为 `26CFED312DAA3BD57696ECEDA5DC0DD38C79A1B024C18B1FCB960070A7670DA9`；安装包 SHA-256 为 `44BE978B8AC4F10EFCBAC514F542255114B6E21B020D931D974C1C7AFD260667`。

### 2026-09-17：每日多次心情记录与日历可视化

- 需求：心情记录不能只剩“好/坏”分数或每天一个扁平类别；需要用可视化方式长期保留一天内的变化，同时保持轻量。
- 实现：每天可保存多条开心、平静、焦虑、难过、生气或疲惫记录，每条包含轻微/明显/强烈三档程度、可选短备注与时间。月历以类别颜色和强度深浅显示最多四个圆点；日历右侧与日记页复用同一记录器和当天时间线，可删除误记。
- 兼容与持久化：新增 `planner.moodEntries` 逐条事件数组，旧资料库归一化时补空；记录随现有资料库、备份和恢复链路保存，不计算“心情分数”或自动心理评价。
- 验证与交付：TypeScript、前端生产构建、Rust Release 与 NSIS 成功；按用户偏好未运行 lint 或测试。桌面 `一隅_直接运行.exe` 已更新并与 Release 源文件哈希一致，SHA-256 为 `F39970A3CDB66005FB0704E47B8BF599DB59E2716B0A0D14635F23BA7A2DA24A`；安装包 SHA-256 为 `3DA96FE731672A23C6F7EFA079A1BC8EC5F53E35732EF553489EF6EAACCF4BF4`。

### 2026-09-17：待办卡片收敛为每日式简洁交互

- 反馈：周期次数卡片把周期、目标、重要程度、撤销与删除全部平铺，信息密度过高；希望日常使用只看到主要动作，低频操作由设置按钮打开。
- 实现：普通待办卡片只保留勾选、标题/备注和设置；周期次数卡片额外保留紧凑标记、当期进度和“记录一次”。执行日期、重复方式、每周日期、周期目标、重要程度、撤销最近一次及删除统一迁入设置弹窗；新建周期任务默认每周 3 次，添加后可调整。统计与历史数据模型未改变。
- 验证与交付：TypeScript、前端生产构建、Rust Release 与 NSIS 成功；未运行 lint 或测试。桌面 `一隅_直接运行.exe` 已更新并与 Release 源文件哈希一致，SHA-256 为 `81D4AF548934F5CC49F754FAA066B58646286440C8F59C1E818FF43B76445361`；安装包 SHA-256 为 `73831157B340173ADDFAE6745F3FC01A48AC19E1277DD599D77D1BA008D4F104`。

### 2026-09-17：待办支持周期目标次数

- 需求：支持“每周跑步 3 次”一类任务，每完成一次产生标记，达到目标后本周期算完成，超过目标仍继续记录，进入下一周期重新计数。
- 实现：新增每天/每周/每月三种周期与 1–99 次目标；每次完成保存独立 ID 和时间，可撤销当前周期最近一次。界面显示标记、当前次数/目标、还差或超额次数、累计记录和进度条；达成后仍留在“今天”方便继续打标，日历摘要同步显示所选周期进度。
- 兼容：现有不重复、每日和每周指定星期任务不变；旧资料库自动补空完成日志。周期汇总实时派生，不写入可能失真的缓存字段，完成日志随现有备份恢复。
- 验证与交付：TypeScript、前端生产构建、Rust Release 与 NSIS 成功；按用户偏好未运行 lint 或测试。桌面 `一隅_直接运行.exe` 已更新并通过复制后 SHA-256 校验，哈希为 `38ADFDDFAD42C516877CFDB2F7EF7975EA92377D29F0E113DF97E4D630428437`；安装包 SHA-256 为 `892B26ED90785C584B494B6A709E106C402C729F4CE88620E9A8CA66C816AD0A`。

### 2026-09-16：答案之书本地随机个人仪式

- 需求：功能名为“答案之书”，默认开启但可在设置关闭；不调用 AI、不自定义语料、不保存全部提问，也不采用洗牌避重。每次独立随机抽取，只有主动收藏才记录问题、答案和时间；后续明确取消“再问一次”。
- 实现：新增独立导航页面、300 条本地原创答案、键盘触发与减少动态效果支持；每个问题只允许抽取一次，翻开后锁定本题并移除重新抽取，“写下新问题”会清空上一页后恢复输入。动画升级为纸页掀起、光影经过、书页落下与墨迹显现；收藏进入统一 `LibraryData` 和备份链路，可查看和删除。旧资料库自动补空收藏与默认开启模块，关闭模块隐藏入口但保留收藏；左侧“创作空间”默认收起、可展开并记住本机选择。
- 验证：TypeScript 检查、前端生产构建、Rust Release 与 NSIS 成功；文档一致性检查通过。按用户偏好未运行 lint 或测试；MSVC 仅输出正常的导入库 linker message。
- 交付：创作空间折叠项已修正为与音乐、帮助中心一致的标准导航尺寸；候选安装包保留在项目 NSIS 目录，SHA-256 为 `126FA8B2C617FBFD03DFA55DA59841763AEA8584B0F1908DB51EFA704D849485`。最新 EXE 已替换桌面的 `一隅_直接运行.exe`，复制后 SHA-256 校验一致，为 `BD30EE00005AF3CFE552AF8EA4B9BB5B9315D2A78418744A3DA257ADFC7C2185`；临时副本已删除。

### 2026-09-14：质量门禁与文档流程精简

- 用户确认：普通开发优先 coding，可不跑自动测试并交给用户实际体验；合并验证、候选打包和正式发布继续分离。
- 已实现：新增 `verify:quick`、`verify:ui`、`verify:full` 和 `release:build`；正式 release 命令仍保留风险匹配验证、安装包、状态与哈希收口。
- 验证策略：`verify:ui` 不再运行测试；前端全量和 Rust 测试只保留在 `verify:full`、跨层高风险和正式大版本。Vitest 在需要时使用最多 2 workers/dot 输出。
- 文档调整：普通修复不再触发全套状态同步或系统审查；强制阅读文件只保留当前规则和近期事实，旧门禁/检查点/审查记录移入 `docs/archive/`。
- 版本规则：本地候选包不登记正式状态；正式补丁命令已强制要求更高的 patch 版本和摘要，拒绝覆盖当前公开版本。
- 验证：自动化脚本 11/11；`verify:quick` 仅运行指定文件的 2/2 测试，约 1.14 秒；`verify:ui` 通过 TypeScript、oxlint、前端 69/69 和 Web build，未生成安装包；文档一致性通过。
- Git：用户授权 Agent 管理仓库；首次基线使用仓库本地身份 `sudeyugan <166131451+sudeyugan@users.noreply.github.com>`，远端为 `sudeyugan/personalized-platform`，分支为 `main`。

### 2026-09-13：新电脑开发环境迁移验证完成

- 用户目标：把下载目录中的“一隅”迁移包恢复到现有路径 `D:\coding\yiyu`，并在本机继续开发。
- 已完成：迁移包 SHA-256 与同名校验文件一致；去除 ZIP 外层重复的 `yiyu/` 后解压；1509 个归档文件逐文件 SHA-256 复核，缺失 0、内容不一致 0。
- 环境：Node `24.19.0`、pnpm `11.20.0`、Rust/Cargo `1.98.1`、Visual Studio 2022 C++ 工具链与 WebView2 Runtime 已可用；前端依赖按 `pnpm-lock.yaml` 冻结恢复。
- 验证证据：TypeScript、oxlint、生产 Web 构建、文档一致性通过；前端 69/69、Rust 26/26 通过。Rust 仅有 MSVC 创建导入库的正常 linker message。
- Git：仍保持 `master` 无提交、项目文件全部未跟踪的原始事实；已把本仓库加入当前用户的 Git `safe.directory`，未代替用户创建首次提交。
- 偏差与边界：无产品、代码、架构或版本偏差；本轮只新增可重建依赖/构建缓存和迁移审查记录。应用个人资料库、密码与 Provider API Key 仍未包含在源码迁移中。
- 下一步：用户可在本机执行 `pnpm tauri:dev` 做桌面自然体验；确认 Git 身份后建立首次基线提交。实际写作数据如需迁移，另从 `.yiyu-backup` 恢复。

### 2026-09-13：跨电脑开发迁移包

- 用户目标：把当前完整项目迁移到另一台电脑继续开发，不改变产品版本、M8 状态或后续 Roadmap。
- 已完成：新增根目录 `开发环境迁移说明.md`，记录 Windows/Node/pnpm/Rust/MSVC 前置、锁文件恢复命令、接手验证、Git 现状和个人资料迁移边界；README 已加入入口。
- 打包范围：保留全部源码、配置、锁文件、中文文档、`.agent/`、`.github/`、`.inputs/` 和当前 `.git/`；排除 `node_modules`、`.pnpm-store`、Vite `dist`、Rust `target`、Tauri `gen`、环境变量、编辑器缓存与日志。
- 证据：最终包实际解压后与源目录逐文件比较 1509 个文件；缺失 0、额外 0、长度/SHA-256 不一致 0，排除目录/文件误入数为 0。外层 ZIP 校验值另存为同目录 `.sha256.txt`。
- Git 事实：当前 `master` 没有提交，工作区项目文件全部未跟踪；迁移不能依赖 clone，压缩包保留现有 `.git`，并建议新电脑验证后立即建立首次提交。
- 偏差分类：无产品、代码、架构或验收偏差；应用个人资料库、DPAPI Provider 密钥和 Rust/Node 构建缓存有意不进入源码迁移包。个人资料需另用 `.yiyu-backup`，API Key 需重新配置。
- 下一步：把 ZIP 与同名校验文件复制到新电脑，按迁移说明恢复依赖并建立首次 Git 提交。

### 2026-09-15：角色局部 Sprite + Slot 兼容演进

- 已完成：角色包支持通用 Slot、资源 offset、旧版全画布回退、缺失 Slot 安全降级、开发模式 Slot 边界和 Renderer 类型注册入口；控制器、状态机、眨眼与固定动作逻辑未改。
- 素材边界：眼睛、眉毛、嘴巴和覆盖层可改用局部透明 PNG；动作序列帧仍保持完整角色画布。Slot 宽高只作为参考与提示，渲染始终使用图片原始尺寸。
- 验证事实：修改过程中 TypeScript 检查通过，角色 Loader/Renderer 定向用例 8/8 通过；按用户随后明确要求，没有继续运行完整 lint、全量 test 或 build。
- 下一步：使用实际母版与局部五官素材调整 Slot 坐标，用户自然体验后再处理具体错位或素材一致性问题。

### 2026-09-15：M9 日常记录与规划启动

- 范围变化：用户暂时搁置虚拟形象素材制作，明确要求新增日记兼容课表和 Todo List 两个页面；目标基线与路线图已更新为 M9。
- 已实现：新增“日记与课表”双视图和“待办”独立页面；课表为七天六节网格并支持同格多课，日记按日期关联当天课程，待办支持筛选、截止日期、优先级、备注和完成状态。
- 数据：三类数据写入现有 LibraryData，由同一资料库与备份链路保存；旧资料库归一化为空数组，不读取或预置截图中的个人信息与课程。
- 验证事实：功能完成时仅做静态复查；随后应用户要求执行一次本地 Tauri Release 构建，内含 TypeScript、生产 Web、Rust Release 与 NSIS 打包并全部成功。未运行 lint 或测试，当前阶段仍保持“进行中”，交给自然体验发现 UI 与交互问题。
- 下一步：用户运行开发预览体验两个入口；后续可根据实际需要补课程时间自定义、学期/单双周、日记日历或待办分组，不预先扩大本轮范围。

### 2026-09-15：M9 日历、截图课表与重复待办补充

- 需求纠正：主入口由“日记与课表”调整为“日历与课表”；日记作为日期内容保留，不删除既有数据。
- 已完成：默认月历显示每日课程数、未完成待办与日记标记，日期侧栏展示当天安排；课表按截图识别 13 条课程并通过 courseImportVersion=1 一次性导入，按星期/节次/标题去重。
- 待办：支持不重复、每天、工作日、每周；重复任务使用 completedDates 按日期完成，第二天或下个周期重新出现；界面仍只保留今天、全部、今日已完成三个筛选。
- 验证：本地 Tauri Release 构建成功，内含 TypeScript、Vite、Rust Release 与 NSIS；未运行 lint 或测试。链接器只有正常的 MSVC 导入库创建信息。
- 下一步：安装桌面最新包后启动资料库，归一化迁移会写入截图课表；自然体验月历密度、课程文字和 daily task，再针对真实问题调整。

### 2026-09-15：日记归入创作空间

- 已完成：日记从“日历与课表”的内部页签拆成创作空间独立页面；日历保留日记标记和按选中日期跳转，课程侧栏继续作为写作背景。
- 兼容：`journal` 旧导航 ID 与旧活动页面自动迁移为 `calendar`；日记仍使用原有 diaryEntries，不复制、不删除数据；写作模块关闭时日记入口同步隐藏。
- 结构：月历/课表、日记分别由 CalendarScheduleView 与 DiaryView 承担，共享日期和课表常量，避免继续扩大单一页面职责。
- 验证：TypeScript 检查通过；未运行 lint、测试、Rust 或安装包构建。
- 下一步：用户自然体验导航归属与日历跳转；如需桌面安装包，可在下一次明确要求时再构建。

### 2026-09-15：日历事务、学期周次与待办重复优化

- 日历：月历格不再显示课程数和待办数，只显示用户新增的日程事务与轻量日记标记；右侧摘要保留当日事务、课程、待办和日记入口。
- 学期：以 2026-09-14 为第 1 周起点，共 16 周；右侧课程按所选日期的学期周次和课程自身周次配置过滤，学期外不显示固定课程。
- 待办：创建和编辑都改为按规则展示控件；不重复显示执行日期，每日不显示日期，每周可以直接选择任意星期组合。旧“工作日”自动迁移为周一至周五，旧“每周”保留原星期。
- 数据：新增 calendarEvents 与 term，继续进入原资料库、备份和恢复链路；旧存档归一化时安全补齐。
- 验证：仅运行一次桌面端 TypeScript 检查并通过；未运行 lint、测试、Rust 或安装包构建。

### 2026-09-16：日记聚焦与重复待办统计

- 日记：移除当天课程侧栏和相关说明，页面收敛为居中的单栏日期日记编辑器；日历中的课程摘要不受影响。
- 待办：每日和每周任务从创建日至今天按实际有效执行日计算计划次数，并结合 completedDates 显示累计完成、完成/计划和百分比进度；不重复任务不显示周期统计。
- 数据：统计结果实时派生，不新增持久化字段，既有重复任务自动生效且不扩大备份内容。
- 验证：仅运行桌面端 TypeScript 检查并通过；未运行 lint、测试、Rust 或安装包构建。

### 2026-09-16：智能创作设置界面整合

- UI：把原先同时纵向堆叠的 AI 服务、伙伴权限、形象/记忆/成长三张长卡片整合为统一控制台，通过三个带状态摘要的入口切换，一次只展示当前区域。
- 层级：顶部明确“本地优先、主动触发、权限可撤销”三条边界；记住上次选中的智能创作分区，窄窗口自动切换为纵向导航。
- 兼容：仅调整 React 组件组织与 CSS，不修改 Provider、密钥、权限、角色包、记忆、成长数据或业务动作。
- 验证：桌面端 TypeScript 检查通过；按用户持续偏好继续构建前端生产包与 Windows NSIS 候选安装包。

### 2026-09-16：本地交付改为桌面直接运行版

- 原因：当前 EXE 与 NSIS 均无 Authenticode 签名，本机 Windows 应用控制提示安装包缺少签名；彻底消除签名警告需要受信任的代码签名证书，自签名不能可靠替代。
- 已处理：最新 Release `yiyu.exe` 已复制为桌面 `一隅_直接运行.exe`，SHA-256 为 `2A63FC2E97144FC5872C08D9B086FC771E8E2E4A28D70ABE5FFB36CC4DF314C5`；智能创作 UI 安装包已从桌面移入 `target/release/bundle/nsis/local-candidates/`，项目内通用 NSIS 产物继续保留。
- 后续流程：每次代码修改后自动构建 Release，更新桌面的直接运行 EXE；NSIS 仅放项目构建目录。纯文档任务不构建，除非用户另有要求。
- 风险：直接运行版同样显示 NotSigned；若系统策略禁止所有未签名 EXE，仍需用户调整 Windows 应用控制或以后配置正式签名证书。
