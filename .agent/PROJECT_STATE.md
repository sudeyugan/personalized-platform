# Project State

<!-- generated:project-status:start -->
最后更新：2026-09-15  
当前版本：`2.0.0`  
当前阶段：M9 日常记录与规划  
阶段状态：进行中  
下一阶段：后续方向待用户确认  
自动门禁：未通过  
人工验收：待验收
<!-- generated:project-status:end -->

## 状态摘要

- M7 `1.2.0` 已由用户继续 M8 的指令自然验收；M8 `2.0.0` 的桌面角色 Runtime 与退出生命周期同版本反馈补丁已交付待自然体验。
- 音频副本由 Rust 可信仓储管理并进入完整备份；支持有序队列、音量、三种循环、快捷键、跨页面播放和默认关闭的自动上下文切换。
- 伙伴默认无权读取资料；加密作品在持久作品/章节授权之外还需仅本次解锁会话有效的内存许可，锁定或撤销立即失效。
- 本地 Mock 不联网；自定义 provider 未选择兼容协议时明确拒绝发送。桌面伙伴只读取主窗口注册的当前角色包显示素材白名单，不允许 Runtime 或成长机制扩大资料访问。
- 最新检查：本次完整补丁门禁前端 69/69、Rust 26/26、TypeScript、无警告 oxlint、生产双窗口 Tauri/NSIS、文档与哈希通过。

## 本阶段完成

- [x] 本地音乐：白名单复制入库、元数据、回收站、永久删除与备份恢复。
- [x] 播放器：有序队列、音量、停止/列表循环/单曲循环、快捷键与跨页面持续播放。
- [x] 上下文：全局、作品、章节、专注四层回退；手动队列准确，自动切换默认关闭。
- [x] 伙伴侧栏：名称、静态表情、最近对话、本地 Mock 和可替换 provider 安全边界。
- [x] 权限中心：作品、章节、资料和音乐逐项授权；加密作品额外临时许可与撤销/锁定清理。
- [x] 兼容与发布：旧资料迁移、音频完整备份、失败路径、生产构建、NSIS 和 SHA-256 校验。

## 待人工验收

- [ ] 安装最新 `2.0.0` 后自然体验桌面伙伴退出、角色文件夹导入、差分/动作播放、记忆和成长；无固定场景、环境矩阵或连续天数要求，发现问题时再记录和针对性回归。

## 实现差异与技术债

| 项目 | 当前实际 | 等级 | 处理计划 |
|---|---|---:|---|
| SQLite 权威模型 | 资料、素材与 AI 表为可查询投影，权威写入仍是版本化 `library_state` 快照 | 1 | 1.0 保持兼容；无性能或一致性证据前不做破坏性逐实体改写 |
| i18n | 导航和资料共用文案已集中，但设置、首次向导、帮助及 M7 写作工具栏/音乐/伙伴仍有组件内中文 | 2 | 不影响简中交付；M8 扩大伙伴 UI 或工具栏继续增长前按 writing/companion/music 领域提取，禁止无边界扩张 |
| 文档转换 | 基础中文标题、正文、列表、引用和图片可迁移；复杂浮动图、分页与字体不能保证往返一致 | 2 | UI/发布说明明示限制，Markdown＋图片为长期开放兜底 |
| Windows 会话事件 | 已覆盖超时、窗口隐藏、失焦和退出；没有自动化真实锁屏/休眠驱动 | 2 | 不设固定人工矩阵；真实使用中发现问题时再做针对性回归 |
| 自动桌面 E2E | M7 音频由组件状态、Rust 文件/备份和生产构建覆盖，未引入 Tauri driver | 2 | 当前收益不足以覆盖驱动维护；M8 第二窗口 capability/多屏行为前必须重新评估 |
| 资料库迁移位置 | 首次建库可选目录，但已有资料库不能直接原地搬迁 | 2 | 通过完整备份在新空资料库恢复；发布说明明确限制 |
| 自定义伙伴 provider | 接口、HTTPS 配置和安全失败已实现，未绑定具体在线协议 | 2 | 用户选择服务/协议并完成隐私决策后单独适配；在此之前绝不发送 |
| 音频编码兼容 | 白名单格式仍可能包含 WebView2 无法解码的特殊编码或损坏文件 | 2 | 失败不影响写作；根据真实文件样本补解码提示或转码方案 |

## 下一步

1. 安装最新 `2.0.0`，自然体验主窗口退出与实际 Image 2 角色包。
2. M8 之后的正式方向仍待用户确认；不自动进入云同步、插件市场或在线音乐候选范围。
3. 仍不建议把唯一一份重要原稿交给任何单一应用或设备。

## 最近检查点

### 2026-09-14：质量门禁与文档流程精简

- 用户确认：普通开发优先 coding，可不跑自动测试并交给用户实际体验；合并验证、候选打包和正式发布继续分离。
- 已实现：新增 `verify:quick`、`verify:ui`、`verify:full` 和 `release:build`；正式 release 命令仍保留风险匹配验证、安装包、状态与哈希收口。
- 验证策略：`verify:ui` 不再运行测试；前端全量和 Rust 测试只保留在 `verify:full`、跨层高风险和正式大版本。Vitest 在需要时使用最多 2 workers/dot 输出。
- 文档调整：普通修复不再触发全套状态同步或系统审查；强制阅读文件只保留当前规则和近期事实，旧门禁/检查点/审查记录移入 `docs/archive/`。
- 版本规则：本地候选包不登记正式状态；正式补丁命令已强制要求更高的 patch 版本和摘要，拒绝覆盖当前公开版本。
- 验证：自动化脚本 11/11；`verify:quick` 仅运行指定文件的 2/2 测试，约 1.14 秒；`verify:ui` 通过 TypeScript、oxlint、前端 69/69 和 Web build，未生成安装包；文档一致性通过。
- Git：用户授权 Agent 管理仓库；首次基线使用仓库本地身份 `sudeyugan <166131451+sudeyugan@users.noreply.github.com>`，远端为 `sudeyugan/personalized-platform`，分支为 `main`。

### 2026-09-13：新电脑开发环境迁移验证完成

- 用户目标：把下载目录中的“一隅”迁移包恢复到现有路径 `D:\coding\yiyu`，并在本机继续开发。
- 已完成：迁移包 SHA-256 与同名校验文件一致；去除 ZIP 外层重复的 `yiyu/` 后解压；1509 个归档文件逐文件 SHA-256 复核，缺失 0、内容不一致 0。
- 环境：Node `24.19.0`、pnpm `11.20.0`、Rust/Cargo `1.98.1`、Visual Studio 2022 C++ 工具链与 WebView2 Runtime 已可用；前端依赖按 `pnpm-lock.yaml` 冻结恢复。
- 验证证据：TypeScript、oxlint、生产 Web 构建、文档一致性通过；前端 69/69、Rust 26/26 通过。Rust 仅有 MSVC 创建导入库的正常 linker message。
- Git：仍保持 `master` 无提交、项目文件全部未跟踪的原始事实；已把本仓库加入当前用户的 Git `safe.directory`，未代替用户创建首次提交。
- 偏差与边界：无产品、代码、架构或版本偏差；本轮只新增可重建依赖/构建缓存和迁移审查记录。应用个人资料库、密码与 Provider API Key 仍未包含在源码迁移中。
- 下一步：用户可在本机执行 `pnpm tauri:dev` 做桌面自然体验；确认 Git 身份后建立首次基线提交。实际写作数据如需迁移，另从 `.yiyu-backup` 恢复。

### 2026-09-13：跨电脑开发迁移包

- 用户目标：把当前完整项目迁移到另一台电脑继续开发，不改变产品版本、M8 状态或后续 Roadmap。
- 已完成：新增根目录 `开发环境迁移说明.md`，记录 Windows/Node/pnpm/Rust/MSVC 前置、锁文件恢复命令、接手验证、Git 现状和个人资料迁移边界；README 已加入入口。
- 打包范围：保留全部源码、配置、锁文件、中文文档、`.agent/`、`.github/`、`.inputs/` 和当前 `.git/`；排除 `node_modules`、`.pnpm-store`、Vite `dist`、Rust `target`、Tauri `gen`、环境变量、编辑器缓存与日志。
- 证据：最终包实际解压后与源目录逐文件比较 1509 个文件；缺失 0、额外 0、长度/SHA-256 不一致 0，排除目录/文件误入数为 0。外层 ZIP 校验值另存为同目录 `.sha256.txt`。
- Git 事实：当前 `master` 没有提交，工作区项目文件全部未跟踪；迁移不能依赖 clone，压缩包保留现有 `.git`，并建议新电脑验证后立即建立首次提交。
- 偏差分类：无产品、代码、架构或验收偏差；应用个人资料库、DPAPI Provider 密钥和 Rust/Node 构建缓存有意不进入源码迁移包。个人资料需另用 `.yiyu-backup`，API Key 需重新配置。
- 下一步：把 ZIP 与同名校验文件复制到新电脑，按迁移说明恢复依赖并建立首次 Git 提交。

### 2026-09-15：角色局部 Sprite + Slot 兼容演进

- 已完成：角色包支持通用 Slot、资源 offset、旧版全画布回退、缺失 Slot 安全降级、开发模式 Slot 边界和 Renderer 类型注册入口；控制器、状态机、眨眼与固定动作逻辑未改。
- 素材边界：眼睛、眉毛、嘴巴和覆盖层可改用局部透明 PNG；动作序列帧仍保持完整角色画布。Slot 宽高只作为参考与提示，渲染始终使用图片原始尺寸。
- 验证事实：修改过程中 TypeScript 检查通过，角色 Loader/Renderer 定向用例 8/8 通过；按用户随后明确要求，没有继续运行完整 lint、全量 test 或 build。
- 下一步：使用实际母版与局部五官素材调整 Slot 坐标，用户自然体验后再处理具体错位或素材一致性问题。

### 2026-09-15：M9 日常记录与规划启动

- 范围变化：用户暂时搁置虚拟形象素材制作，明确要求新增日记兼容课表和 Todo List 两个页面；目标基线与路线图已更新为 M9。
- 已实现：新增“日记与课表”双视图和“待办”独立页面；课表为七天六节网格并支持同格多课，日记按日期关联当天课程，待办支持筛选、截止日期、优先级、备注和完成状态。
- 数据：三类数据写入现有 LibraryData，由同一资料库与备份链路保存；旧资料库归一化为空数组，不读取或预置截图中的个人信息与课程。
- 验证事实：功能完成时仅做静态复查；随后应用户要求执行一次本地 Tauri Release 构建，内含 TypeScript、生产 Web、Rust Release 与 NSIS 打包并全部成功。未运行 lint 或测试，当前阶段仍保持“进行中”，交给自然体验发现 UI 与交互问题。
- 下一步：用户运行开发预览体验两个入口；后续可根据实际需要补课程时间自定义、学期/单双周、日记日历或待办分组，不预先扩大本轮范围。

### 2026-09-15：M9 日历、截图课表与重复待办补充

- 需求纠正：主入口由“日记与课表”调整为“日历与课表”；日记作为日期内容保留，不删除既有数据。
- 已完成：默认月历显示每日课程数、未完成待办与日记标记，日期侧栏展示当天安排；课表按截图识别 13 条课程并通过 courseImportVersion=1 一次性导入，按星期/节次/标题去重。
- 待办：支持不重复、每天、工作日、每周；重复任务使用 completedDates 按日期完成，第二天或下个周期重新出现；界面仍只保留今天、全部、今日已完成三个筛选。
- 验证：本地 Tauri Release 构建成功，内含 TypeScript、Vite、Rust Release 与 NSIS；未运行 lint 或测试。链接器只有正常的 MSVC 导入库创建信息。
- 下一步：安装桌面最新包后启动资料库，归一化迁移会写入截图课表；自然体验月历密度、课程文字和 daily task，再针对真实问题调整。

### 2026-09-15：日记归入创作空间

- 已完成：日记从“日历与课表”的内部页签拆成创作空间独立页面；日历保留日记标记和按选中日期跳转，课程侧栏继续作为写作背景。
- 兼容：`journal` 旧导航 ID 与旧活动页面自动迁移为 `calendar`；日记仍使用原有 diaryEntries，不复制、不删除数据；写作模块关闭时日记入口同步隐藏。
- 结构：月历/课表、日记分别由 CalendarScheduleView 与 DiaryView 承担，共享日期和课表常量，避免继续扩大单一页面职责。
- 验证：TypeScript 检查通过；未运行 lint、测试、Rust 或安装包构建。
- 下一步：用户自然体验导航归属与日历跳转；如需桌面安装包，可在下一次明确要求时再构建。

### 2026-09-15：日历事务、学期周次与待办重复优化

- 日历：月历格不再显示课程数和待办数，只显示用户新增的日程事务与轻量日记标记；右侧摘要保留当日事务、课程、待办和日记入口。
- 学期：以 2026-09-14 为第 1 周起点，共 16 周；右侧课程按所选日期的学期周次和课程自身周次配置过滤，学期外不显示固定课程。
- 待办：创建和编辑都改为按规则展示控件；不重复显示执行日期，每日不显示日期，每周可以直接选择任意星期组合。旧“工作日”自动迁移为周一至周五，旧“每周”保留原星期。
- 数据：新增 calendarEvents 与 term，继续进入原资料库、备份和恢复链路；旧存档归一化时安全补齐。
- 验证：仅运行一次桌面端 TypeScript 检查并通过；未运行 lint、测试、Rust 或安装包构建。

### 2026-09-16：日记聚焦与重复待办统计

- 日记：移除当天课程侧栏和相关说明，页面收敛为居中的单栏日期日记编辑器；日历中的课程摘要不受影响。
- 待办：每日和每周任务从创建日至今天按实际有效执行日计算计划次数，并结合 completedDates 显示累计完成、完成/计划和百分比进度；不重复任务不显示周期统计。
- 数据：统计结果实时派生，不新增持久化字段，既有重复任务自动生效且不扩大备份内容。
- 验证：仅运行桌面端 TypeScript 检查并通过；未运行 lint、测试、Rust 或安装包构建。
