# Learnings and Process Improvements

> 保存可跨任务复用的经验，不记录一次性的过程流水账。

## 有效模式

| 日期 | 模式 | 证据 | 应用范围 | 是否固化 |
|---|---|---|---|---|
| 2026-08-08 | 发布状态必须拆分“代码完成 / 自动验证 / 人工验收” | M2 自动门禁通过但仍有 Windows/IME 验收 | 所有里程碑交付 | 是 |
| 2026-08-08 | 产品基线与实现状态采用“原文 + 实现注记”，避免重写基线 | `docs/product` 与 `.agent` 职责不同 | 文档同步 | 是 |
| 2026-08-09 | 旧快照升级关系模型时先做事务性可重建投影 | M3 schema v4 无损兼容 `0.1.0` | 渐进数据库迁移 | 是 |
| 2026-08-09 | 路线图按足量用户闭环而不是原文档编号发布 | 原 M4 单独工作量不足，合并 M5 后形成图片到安全迁移闭环 | 后续版本规划 | 是 |
| 2026-08-09 | 文件二进制和秘密放可信端，前端只编排逻辑 ID 与显式授权 | M4–M5 图片、DPAPI、备份和 vault 边界 | 文件/安全功能 | 是 |
| 2026-08-11 | 人工验收应与可发现风险成比例，自动门禁已覆盖的内容不再要求用户重复打卡 | 用户明确反馈十项场景、七天试用和环境矩阵负担过重 | 阶段交付与发布验收 | 是 |
| 2026-08-11 | 用户可控的正文语义标记可以同时服务资料建档、反向引用和外发脱敏 | `entityReference` mark 与 AI 实际发送预览共用同一语义源 | 编辑器资料化与 AI 隐私 | 是 |
| 2026-08-11 | 危险操作确认应先说明可恢复性和影响范围，并把默认焦点留给安全操作 | 删除弹窗统一后不再依赖系统原生文案，回收站与永久删除可明确区分 | 所有删除、覆盖和不可逆操作 | 是 |
| 2026-08-12 | “用户已解锁”与“AI/伙伴可读取”必须是两层独立授权 | M7 加密作品需持久范围授权＋仅内存临时许可，锁定即撤销 | 所有加密内容的 AI、插件与未来桌面窗口 | 是 |
| 2026-08-12 | 持续播放应把音频生命周期放在 Shell，把队列规则留在纯领域函数 | 页面切换/隐藏控件不卸载 audio，四层回退和手动队列可单测 | 音乐、语音与其他跨页面媒体 | 是 |
| 2026-08-13 | UI “字号过大”应先排查继承和信息层级，不只缩某一个标题 | 两份独立审查都定位到未设字号控件继承 16px，并发现卡片/表单语法混用 | 设置页与后续 M8 伙伴 UI | 是 |
| 2026-08-13 | 响应式隐藏整块功能时必须提供等价入口 | ≤1020px 隐藏右栏曾让伙伴完全不可达，补抽屉后恢复能力 | 所有可折叠侧栏与桌面/移动布局 | 是 |
| 2026-08-16 | 开发内循环和发布收口应分级，已由发布命令覆盖的门禁不要人工预跑一遍 | M7 UI 补丁先通过类型/lint/定向测试，随后完整发布再次重复静态与跨层门禁 | 同版本补丁和后续里程碑开发 | 是 |
| 2026-08-16 | 同一编辑器生态的扩展必须与 core 精确对齐版本，不能用会跨小版本解析的范围 | Tiptap task-item `3.30.1` 请求现有 core `3.29.2` 未导出的 API，测试在模块加载期失败 | Tiptap 与其他强耦合插件生态 | 是 |
| 2026-08-16 | 用户要求“随页面大小”时不能擅自保留阅读宽度上限，且视觉线框要检查相邻布局边界而不只检查内容节点 | 1120px 上限和左右侧栏 border 自动测试通过，但用户全屏复测仍认为可写范围受限且中部有线 | 编辑器、画布、看板等全空间交互 UI | 是 |
| 2026-08-19 | 多窗口应用的主窗口关闭语义必须在原生生命周期层统一，不能假设关闭一个 Webview 等于退出进程 | companion 静态窗口继续存活使主程序关闭后仍驻留后台；Tauri `CloseRequested` 统一退出后语义明确 | 所有托盘、悬浮窗、预览窗和多窗口桌面功能 | 是 |
| 2026-08-19 | 高帧数可替换视觉资源应传逻辑清单并使用最小读取白名单，不应把整包字节塞进跨窗口状态事件 | 差分/序列帧角色包最多数十张大 PNG，事件只传配置且 companion 只能读取当前包素材 | 多窗口图片、音视频和插件资源 | 是 |
| 2026-09-14 | 开发验证、候选打包和正式发布必须解耦，文档只在事实变化时更新 | 旧 UI 补丁每次都重跑全量前端、Tauri/NSIS、哈希和多文档收口；69 项测试的 verbose/单 worker 也放大时间与输出 | 所有后续开发与发布 | 是 |

## 失败与修正

| 日期 | 问题 | 根因 | 修正 | 防止复发 |
|---|---|---|---|---|
| 2026-08-08 | 完成 M2 后只更新了反馈与 spike 文档 | 结束审查没有逐项检查动态文档，且把自动通过误写成全部完成 | 增加里程碑文档同步清单并补齐所有状态 | 每次交付前核对 README、产品状态注记和全部 `.agent` 动态文件 |
| 2026-08-16 | 首轮正文响应式修正自动门禁通过但未满足用户结果 | 把“更宽”理解成保留 1120px 阅读上限，且只检查正文节点、遗漏左右侧栏分隔线 | 取消最大宽度和相邻边线；把前次记录明确标记为被复测推翻 | 对明确的全画布诉求按结果实现；视觉问题审查内容、父容器和相邻区域 |

## 待优化流程

- [x] 已建立 `.agent/project-status.json`、文档检查和正式发布门禁。
- [x] 已将日常定向验证、UI/full 合并验证、候选打包和正式发布解耦，并归档历史流水。

## 固化规则

当经验满足以下条件之一时，将其升级：

- 重复出现两次以上的错误：考虑加入 `AGENTS.md` 或机械检查。
- 稳定且重复的工作流：考虑创建 Skill。
- 可机械判断的问题：优先加入测试、lint、类型检查、hook 或 CI。
- 影响长期架构的结论：记录到 `DECISIONS.md`。
- 新的持续性风险：记录到 `RISKS.md`。

## 阶段复盘模板

```text
阶段：
达到的结果：
未达到的结果：
最有效的做法：
最大的浪费：
发生的偏差及判断：
应保留的流程：
应删除或简化的流程：
应自动化的检查：
下一阶段调整：
```
