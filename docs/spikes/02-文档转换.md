# 文档转换 spike

结论：调整。自动 fixture 已覆盖中文标题、图片、列表、引用和分页，并验证生成 DOCX 可由 Mammoth 再次打开；PDF 验证生成、重开和中文元数据；Markdown 验证透明源文本。DOCX 的浮动图片和复杂样式仍无法保证往返一致，PDF 应定义为排版导出而非可逆格式。因此 M2 不暴露伪可靠入口，M5 再开放。失败样例：浮动图片位置、分页符语义、字体替换和嵌套列表层级。验证库：docx（MIT）、Mammoth（BSD-2-Clause）、pdf-lib（MIT）。
