# 编辑器性能 spike

结论：暂采用 Tiptap。自动基线覆盖 10 万中文字符的文档构造、字数计算和 JSON 序列化，预算为 1 秒；它不等同于 Tiptap 渲染和输入性能。真实中文 IME、撤销/重做、图片块和 10 万字滚动仍以阶段验收机人工走查为准。图片目标结构使用文档块引用资源，不把二进制写入正文 JSON。长章若在低配机器出现持续输入延迟，产品策略是提示拆章，而不是隐藏数据损失。
