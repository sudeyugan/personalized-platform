# Project State 历史检查点

> 归档于 2026-09-14。当前状态见 [.agent/PROJECT_STATE.md](../../.agent/PROJECT_STATE.md)。本文件仅供追溯。

### 2026-08-21：Windows Installer 疑似孤立缓存隔离完成

- 用户确认：选择方案 1，只处理注册表中无 `LocalPackage` 引用的疑似孤立 MSI/MSP，不迁移整个 Installer 目录、不创建软链接。
- 已完成：重新核验候选集为 96 个、29,226,562,560 字节；复制到 `D:\WindowsInstaller-Quarantine\2026-08-21`，逐文件比较源/目标长度与 SHA-256，全部通过后才删除对应 C 盘源文件。
- 验证证据：隔离区 96 个文件、29,226,562,560 字节，`manifest.csv` 96 行并记录原路径、目标路径、时间、属性、ACL SDDL 与 SHA-256；`migration.log` 报告成功。`C:\Windows\Installer` 仍为普通目录而非链接，保留 450 个已注册引用文件、约 4.917 GB，动态复查未引用文件为 0。
- 空间结果：C 盘空闲由约 0.59 GB 增至 27.79 GB；D 盘空闲约 121.48 GB。迁移期间及复核时均无 `msiexec` 进程。
- 回滚与风险：隔离文件未永久删除，可依据清单用管理员权限恢复到原路径；在完成常用软件更新、修复和卸载验证前不要删除 D 盘隔离区。注册表引用比对不能等价于微软官方孤立缓存判定，残余风险继续明确保留。
- 恢复说明：优先根据 Installer 错误指出的缓存文件名恢复单个 MSI/MSP；需要全量回滚时，先保证 C 盘除当前空闲外再增加至少 5 GB 余量，然后以管理员 PowerShell 预校验隔离文件 SHA-256，再复制回清单中的 `OriginalPath` 并恢复时间、属性与 ACL。当前约 27.79 GB 空闲仅勉强容纳 27.219 GB 全量文件，不适合直接全量恢复。
- 项目影响：无产品代码、范围、架构或验收状态变化；一次性高权限脚本已删除，避免误重复执行。

### 2026-08-21：Windows Installer 迁移方案待确认

- 用户意向：希望把 `C:\Windows\Installer` 迁移到 D 盘，并提出使用软链接技术。
- 已核实：C、D 均为 NTFS；D 盘约 701.64 GB、可用约 148.70 GB，容量满足。Windows `mklink /J` 支持目录联接，但微软仅确认联接机制，并明确要求不要删除或破坏 Installer 缓存，没有提供把该系统目录迁移到其他卷的受支持流程。
- 当前状态：未复制、移动、重命名、删除或创建联接；中断前后均只执行只读检查。`C:\Windows\Installer` 仍是原始系统目录，D 盘目标目录不存在。
- 风险与选择：整目录迁移约 32.4 GB 可释放最多空间，但会把软件更新、修复和卸载依赖绑定到 D 盘及联接可用性，属于高风险非标准系统布局；较稳妥方案仍是只隔离约 27.2 GB 的疑似孤立 MSI/MSP，同时保留约 4.9 GB 已注册引用缓存原位。
- 待确认：用户需明确选择“只隔离疑似孤立文件”或“接受风险迁移整个目录并建立联接”。整目录方案执行前还需要交互式管理员权限。

### 2026-08-21：C 盘空间只读诊断

- 触发原因：用户报告系统盘无可用空间，并授权只读检查整个 C 盘；本次不属于产品范围变化，也未修改、删除或迁移任何系统/用户文件。
- 诊断证据：C 盘总计 200 GB，检查期间可用空间约 0.59–0.77 GB；主要逻辑占用为 Windows 80.8 GB、Users 51.7 GB、Program Files 20.3 GB、Program Files (x86) 21.9 GB、ProgramData 20.1 GB。
- 关键发现：当前账户 AppData 约 40.1 GB；可再生缓存候选包括飞书约 4.13 GB、WPS PDF 约 1.79 GB、VS Code 扩展包/崩溃缓存约 1.59 GB、NVIDIA DXCache 约 1.25 GB、VRChat 纹理缓存约 1.44 GB。`C:\Windows\Installer` 约 32.4 GB，其中 96 个 MSI/MSP、合计约 27.2 GB 未出现在 Installer `LocalPackage` 注册引用中，属于高价值但高风险的疑似孤立缓存，禁止直接删除。
- 权限限制：当前执行环境即使申请提升也未获得 Windows 管理员令牌，因此无法完成 DISM 组件存储、影子副本和受保护 `Windows.old` 的核验；普通权限可见回收站约 0.5 MB、Windows 更新下载约 0.11 GB，均非主要原因。
- 偏差与状态：无产品目标、代码、架构或验收偏差；M8 仍保持已交付待人工验收。本次系统维护诊断只形成清理建议，后续删除、卸载或移动需用户另行确认。
- 下一步：优先关闭相关应用后清理明确可再生缓存，目标先释放约 10 GB；再按使用情况卸载 Lenovo/CUDA/Nsight/旧 SDK 等软件。Installer 疑似孤立文件仅允许先移动到其他盘隔离并验证，不做直接永久删除。

### 2026-08-19：2.0.0 桌面角色 Runtime 与退出反馈补丁

- 用户反馈：关闭主程序后伙伴窗口仍让进程驻留；伙伴形象应按差分立绘＋固定动作序列帧任务说明实现，并在页面提供图片入口和 Image 2 指南。
- 已完成：Tauri 主窗口关闭统一退出应用；标准角色文件夹导入、JSON/缺图/同画布/FPS/自然帧序校验；差分渲染、眨眼、说话、待机、固定动作和冲突优先级；设置预览与帮助指南。
- 安全边界：companion 仍不能调用通用素材或资料命令，只能读取主窗口注册在 Rust 内存中的当前角色包逻辑 ID；应用重启后重新注册，其他素材不可枚举。
- 自动证据：受影响前端 19/19、Rust 生命周期与角色素材标识守卫各 1/1；完整补丁门禁前端 69/69、Rust 26/26、TypeScript、无警告 oxlint、生产双窗口 Tauri/NSIS、文档一致性与安装包哈希全部通过。
- 偏差分类：DEC-012 从“不读取素材”调整为“只读取当前角色包白名单显示素材”，这是实现真实桌面立绘所需且由本次用户需求明确授权的安全边界变化；无资料、AI provider 或加密权限扩大。
- 结构检查：配置、加载、状态、动画控制和渲染按职责分层，新增文件均低于关注阈值；没有把 Runtime 堆入设置总页或主 store。
- 产物：`apps/desktop/src-tauri/target/release/bundle/nsis/一隅_2.0.0_x64-setup.exe`；SHA-256 `8DFE0EC15E97ADFC021B6BB77CCC140E0B4111BE876FB47A6F64073D639B5696`。
- 下一步：安装后自然体验；M8 之后的产品方向待用户确认。

### 2026-08-16：M8 `2.0.0` 启动

- 进入依据：用户明确“改完这个后可以来执行 M8”，M7 行间距补丁已发布且 M7 已标记验证。
- 当前范围：隔离桌面窗口、可治理记忆、形象组合/候选确认、动作状态、性格权重日志与回退，以及明确授权下的习惯互动。
- 安全边界：桌面窗口只接收主窗口裁剪后的外观/动作快照，不获得资料库 command；记忆撤销后不再注入，加密作品仍需本次内存许可且不形成普通持久记忆。
- 偏差分类：无目标偏差；M8 之后没有已授权的新里程碑，云同步、插件市场或在线音乐继续留在未承诺候选池。
- 当前证据：M7 发布门禁通过；M8 尚处实现阶段，自动验收待执行。
- 下一步：先完成数据模型/迁移和桌面窗口 capability，再实现 UI 与状态机并跑定向测试。

### 2026-08-17：M8 核心实现检查点

- 已完成：旧库兼容迁移、发型/服装组合、AI 形象草稿确认、透明桌面窗口、动作快照、可治理记忆、JSON 导出、性格权重/日志/回退，以及明确授权下的对话与音乐成长。
- 权限证据：`companion` 窗口 capability 只含拖动与定向事件；全部资料库/素材/音频/密钥/备份/vault command 在 Rust 入口强制 `WebviewWindow.label() == "main"`。
- 上下文证据：未授权记忆不注入，删除物理移除；来源作品授权失效后不注入；加密来源还需本次内存许可，且 UI 禁止把加密作品对话保存为普通长期记忆。
- 自动验收：M8 定向前端 16/16；完整门禁前端 64/64、Rust 24/24、TypeScript、生产双窗口 Tauri/NSIS 通过；最终无警告 UI 收口再次通过前端 64/64、TypeScript、oxlint、生产构建、文档和哈希。
- 产物：`apps/desktop/src-tauri/target/release/bundle/nsis/一隅_2.0.0_x64-setup.exe`；SHA-256 `77A22789B789A72B362BA2EFA9B5E92ECD00810F2617C888FE9C69D71E2CE672`。
- 偏差分类：组合渲染已覆盖桌面窗口，确认的 AI 正式立绘当前在主窗口设置中展示，桌面窗口不接收素材字节以守住最小权限；属于有安全依据的实现边界，不改变 M8 退出条件。
- 下一步：安装后自然体验 M8；M8 之后的方向待用户决定，不自动进入候选能力。

### 2026-08-16：1.2.0 行间距收尾补丁

- 用户新增反馈：正文需要支持行间距设置；完成本项后可以进入 M8。
- 已实现：新增独立 `LineHeight` Tiptap 扩展，为段落与标题提供可持久化 `lineHeight` 属性；工具栏提供默认、1.25、1.5、1.75、2.0 五档，恢复默认会移除节点属性。
- 兼容边界：旧文稿不含 `lineHeight` 时继续使用现有 CSS 默认行距；不修改资料库 schema、Rust、备份、加密或伙伴权限。
- 自动验收：工具栏定向回归 10/10；最终前端 62/62、TypeScript、无警告 oxlint、Vite/Tauri release、NSIS、文档一致性与安装包哈希全部通过。独立 Rust 测试按纯 UI 门禁省略，生产 Rust release 编译通过。
- 产物：`apps/desktop/src-tauri/target/release/bundle/nsis/一隅_1.2.0_x64-setup.exe`；SHA-256 `A54786CB820D2838AEA63F6E14AC1908D9AB35AF59CE2ABDD3FFF28E83AAA7F6`。
- 偏差分类：M7 同版本可用性补丁，无目标或范围偏差；完成并发布后按用户指令把 M7 视为自然验收并进入 M8。
- 下一步：用户已明确允许完成本项后执行 M8；据此自然验收 M7，并进入 M8 架构审查与完整阶段开发。

### 2026-08-16：1.2.0 无框全宽正文、音乐提示与帮助层级修正

- 用户复测结果：此前把正文上限从 760px 放宽到 1120px 仍没有满足“页面多宽即可写多宽”，编辑区两侧栏的垂直边线也仍形成线框；音乐未选曲提示跨页面常驻且不能关闭；背景图指南不应成为独立内容块。
- 已修正：正文节点彻底取消最大宽度并明确清除 border/outline/box-shadow，占满编辑画布；章节栏和上下文栏的垂直分隔线移除，标题/正文/工具栏保持一致响应式留白。
- 已修正：空播放器提示只在音乐页显示并增加关闭按钮，关闭后本次 AppShell 会话保持隐藏；选曲后的真实播放器与跨页面音频生命周期不变。
- 已修正：删除独立背景指南组件和独立 Markdown 文件，把生成数量、尺寸和完整提示词合并为帮助中心“常用指南”中的一个折叠小项。
- 自动验收：App/帮助中心定向回归 12/12；最终前端 61/61、TypeScript、无警告 oxlint、Vite/Tauri release、NSIS、文档一致性与安装包哈希均通过。独立 Rust 测试按纯 UI 门禁省略，生产 Rust release 编译通过。
- 产物：`apps/desktop/src-tauri/target/release/bundle/nsis/一隅_1.2.0_x64-setup.exe`；SHA-256 `E3C4A10CDBBF16DB8365BB6F52DA5E791E2B2A6E2C7946ABFC06E0A9D37E71E4`。
- 偏差分类：前一补丁实现差异经用户复测确认，本轮为同版本缺陷修复；不涉及存储 schema、Rust、音乐队列/播放逻辑、安全权限或 M8 在线生图范围，无需用户决策。
- 下一步：安装后自然体验缩放下的全宽编辑手感、音乐提示关闭行为和帮助层级；有问题继续写入 `1.2.0` 原章节。

### 2026-08-16：1.2.0 写作响应式、章节删除与背景指南补丁

- 用户原始反馈：全屏正文仍被固定在很窄的范围、长工具栏缩放后显示不全、章节删除入口不可发现，并希望获得 GPT Image 2 全局背景图数量和统一风格提示词。
- 当时实现：正文从固定 760px 放宽到最多 1120px；工具栏使用容器查询折叠次要工具，章节删除保持可见。后续用户复测证明正文上限与侧栏边线仍不符合预期，已由上一条检查点继续修正，不再把该宽度方案视为最终结果。
- 当时指南：帮助中心和独立文档提供“4 张候选 + 2 次微调”与模板；后续按反馈撤销独立展示和独立文件，内容合并进常用指南小项。OpenAI 官方依据与当前单图全局边界保持不变。
- 最终证据：定向 App/帮助测试 12/12；完整门禁前端 61/61、TypeScript、无警告 oxlint、Vite/Tauri release、NSIS、文档一致性与哈希校验通过。Vite 在沙箱内曾因 Windows `spawn EPERM` 无法启动，按测试执行权限在沙箱外通过，不属于代码失败。
- 偏差分类：四项均为 `1.2.0` 同版本可用性/说明补丁，无数据 schema、Rust、存储、安全权限或 M8 范围变化；新增 R-018 监控用户生成背景在不同主题/裁切比例下的可读性。
- 已发布：`apps/desktop/src-tauri/target/release/bundle/nsis/一隅_1.2.0_x64-setup.exe`，SHA-256 `2B3F13830D6C723F890A26DD1BF2DB6A6E1B5A93772E1D44F49AA750E9F181A0`。
- 下一步：安装后自然体验正文宽度、工具栏折叠、章节回收站和生成背景的实际裁切；后续问题继续更新 `1.2.0` 原反馈项。

### 2026-08-16：1.2.0 工具栏创作能力修正

- 用户原始反馈：代码块不适合当前写作场景，应改为高亮、对齐、待办列表或查找替换。
- 已实现：移除代码块与行内代码入口；加入高亮、左/中/右/两端对齐、可勾选待办列表，以及包含匹配数、上下匹配、单次/全部替换的内嵌面板。
- 兼容边界：旧文稿已有代码节点仍保留渲染，避免静默修改历史正文；查找不区分大小写且可跨越粗体等 mark 边界。
- 依赖修正：首次安装的新增扩展被 `^` 解析到 `3.30.1`，与现有 core `3.29.2` 加载失败；现已把四个官方扩展精确锁定到 `3.29.2`，供应链策略检查通过。
- 最终证据：工具栏与查找替换定向回归 12/12、前端全量 60/60、TypeScript、无警告 oxlint、Vite/Tauri release、NSIS、文档一致性与哈希门禁通过；按纯前端范围省略独立 Rust 测试，生产构建完成 Rust release 编译。
- 已发布：`apps/desktop/src-tauri/target/release/bundle/nsis/一隅_1.2.0_x64-setup.exe`，SHA-256 `F5D9EBA1E0280E7F2071936FFA82C0C2C0C984E70F9A20B4DE2410A4FC1B65DF`。
- 偏差分类：用户明确确认的同版本体验修正；新增前端 schema 节点/属性由 Tiptap JSON 正常持久化，不涉及 Rust、数据库迁移、安全权限或 M8 范围。
- 下一步：自然体验高亮、对齐、待办与查找替换；如有具体交互偏差继续更新同一反馈条目。

### 2026-08-16：1.2.0 写作工具栏增强补丁

- 用户原始反馈：希望丰富写作页面工具栏功能；同版本补丁保留在 `1.2.0` 反馈章节。
- 已实现：工具栏按历史、段落、文字、结构、插入/资料分组；新增正文、三级标题、行内代码、代码块、链接编辑/移除、清除格式和列表增减缩进，原图片与资料标记入口不变。
- 交互边界：链接使用工具栏内轻量编辑框，不增加阻断式系统弹窗；普通域名自动补全 HTTPS，编辑区链接不因单击意外打开。
- 最终证据：工具栏定向组件测试 7/7、前端全量 55/55、TypeScript、oxlint、Vite/Tauri release、NSIS、文档一致性与哈希门禁全部通过；按纯前端范围省略独立 Rust 测试，生产构建仍完成 Rust release 编译。
- 已发布：`apps/desktop/src-tauri/target/release/bundle/nsis/一隅_1.2.0_x64-setup.exe`，SHA-256 `21FAD3EDD7DB8F1DF4BDE069652092BD202F522A4B39677858874DF431E3CF5A`。
- 偏差分类：同版本前端可用性增强，不改变 Tiptap 持久化 schema、Rust、存储、安全权限或 M8 范围；无需用户决策。
- 下一步：只需自然体验工具栏；如仍缺少具体编辑能力，继续在本条原始反馈上补充，不新增版本章节。

### 2026-08-13：1.2.0 智能创作与 M7 响应式 UI 补丁

- 用户原始反馈：智能创作区域存在字体过大、与其他界面风格不统一和不美观的问题，并要求可使用 sub-agent 共同检查其他 UI。
- 两份独立只读审查一致定位到未设字号的表单/权限标题继承浏览器 16px、智能创作混用两套表单语法；横向审查另发现 ≤1020px 伙伴入口不可达和音乐小屏网格可能错位。
- 已实现：智能创作统一父子标题、控件字号/高度、间距与轻量分组；伙伴设置按外观/对话/权限组织，范围列表折叠限高，Provider 改为明确的配置检查；伙伴侧栏提升到可读字号并增加窄屏抽屉；音乐小屏使用明确 grid areas 和纵向页头。
- 最终证据：前端 52/52、Rust 24/24、TypeScript、oxlint、Vite/Tauri release、NSIS、文档一致性与哈希门禁全部通过。
- 已发布：`apps/desktop/src-tauri/target/release/bundle/nsis/一隅_1.2.0_x64-setup.exe`，SHA-256 `CE72EF3B1AF713F31738589E1D42F6161895801D5C9FBEBF45E830C6F21B3300`。
- 偏差分类：同版本可用性/视觉缺陷修复，无数据模型、权限语义或 Roadmap 范围变化；不需要用户决策。
- 下一步：只需自然体验本次 UI；后续同版本纯 UI 补丁使用分级门禁，避免重复预跑与无关 Rust 测试。

### 2026-08-16：补丁质量门禁分级

- 已完成：新增显式 `pnpm release:patch:ui`；它一次执行类型检查、lint、前端全量、Tauri 生产构建、NSIS、文档同步和哈希校验，省略与纯 UI 改动无关的独立 Rust 测试。
- 范围保护：默认 `pnpm release:patch` 仍为完整门禁；涉及 Rust、Cargo/Tauri、存储、备份、加密、权限或范围不明确时不得使用 UI 快速模式；新版本始终完整发布。
- 流程变化：开发内循环只跑受影响的定向测试；若马上收口发布，不再提前重复发布命令已包含的类型检查、lint 或全量测试。
- 偏差分类：经用户确认的工程流程优化，不改变产品目标、运行时架构或验收语义；新增 R-016 监控误分类风险。
- 证据：文档自动化测试覆盖 UI/full 命令集合和非法范围拒绝；最终文档一致性与既有安装包哈希检查见本次结束审查。

### 2026-08-12：M7 功能实现与首轮自动验证

- 已实现受控本地音频仓储、曲库/回收站、可排序播放队列、音量/循环/快捷键、跨页面持续播放，以及全局/作品/章节/专注四层上下文和默认关闭的自动切换。
- 已实现写作侧栏伙伴、静态表情、可替换对话 provider、作品/章节/资料/音乐逐项授权；默认无权读取，加密作品还需仅驻留内存的本次解锁许可并可即时撤销。
- 完整备份已纳入音频目录；旧资料规范化会补齐 M7 模块、设置和状态，启动只恢复当前曲目/队列但不自动播放。
- 证据：最终前端 51/51、Rust 24/24、TypeScript、oxlint、Vite/Tauri release、NSIS、文档一致性与哈希门禁全部通过；音频备份恢复、路径/类型校验、权限拒绝和加密临时授权均有自动测试。
- 已发布：`apps/desktop/src-tauri/target/release/bundle/nsis/一隅_1.2.0_x64-setup.exe`，SHA-256 `FD9F4F443F8926F89F75E6F7A3C3304B6A2239012D8E06EB4798745055894224`。
- 正在做：代码与自动验收已完成，阶段状态为“已交付待人工验收”；只等待安装后的自然体验反馈。
- 偏差：自定义伙伴 provider 仅完成 HTTPS 配置边界，尚未绑定用户未选择的服务协议；属等级 2 范围边界，不影响本地 Mock 和核心写作。桌面悬浮伙伴、长期记忆与成长仍按计划留在 M8。
- 用户确认：当前不需要；阶段末仅保留安装后自然体验，不恢复固定人工清单。

### 2026-08-11：1.0.0 删除确认 UI 补丁

- 用户原始反馈：优化人物、地点、时间线等删除弹窗 UI；按“等”扩展到同类的作品、卷、章节和素材删除，不改变删除数据语义。
- 已实现共享 `ConfirmDialog`，明确回收站/永久删除、对象名称、引用或影响与恢复方式；安全操作默认聚焦，支持 Esc、遮罩和关闭按钮取消，并限制 Tab 焦点在对话框内。
- 验证：前端 43/43、Rust 22/22、TypeScript、oxlint、Vite/Tauri release 和 NSIS 通过；共享确认框覆盖安全焦点、Tab 焦点限制、Esc 取消、人物/事件集成与应用回归。
- 安装包：`apps/desktop/src-tauri/target/release/bundle/nsis/一隅_1.0.0_x64-setup.exe`；SHA-256 `788390F4404A46D477D1F72A82D8917D23B4CE9F186E003322087AC226AE9EAE`。
- 偏差分类：同版本 UI 优化，无产品范围或数据模型变化；共享组件避免各页面继续复制危险操作逻辑。
- 下一步：安装后自然体验删除/恢复流程；如有问题继续更新 `1.0.0` 原反馈章节，用户确认后可进入 M7。

### 2026-08-11：1.0.0 验收简化与正文资料标记补丁

- 用户确认取消十项场景、七天试用和固定人工矩阵；基线改为自动门禁优先，安装后自然体验，有问题再反馈。
- 已实现正文人物/地点/事件 mark、关联已有资料、选词直接建档、章节反向引用，以及 AI 印象图发送前稳定匿名替换和实际预览。
- 已删除帮助页打卡 UI、试用动作和新资料中的 `settings.acceptance`；旧资料的历史字段被兼容忽略。
- 验证：前端 41/41、Rust 22/22、TypeScript、oxlint、Vite/Tauri release、NSIS 和文档一致性全部通过；主入口 250.48 kB。
- 安装包：`apps/desktop/src-tauri/target/release/bundle/nsis/一隅_1.0.0_x64-setup.exe`；SHA-256 `79AA6B69DB28CC4CDC173FBA1943573793915BF6E42E63F1F875EE4D7A235DC2`。
- 偏差分类：验收方式为用户已确认的范围变化（DEC-009）；AI 仅能脱敏主动标记的文字，未标记敏感信息仍需在发送预览中检查（R-012）。

### 2026-08-09：1.0.0 M6 自动交付

- 完成首次向导、可配置资料库根目录、帮助与验收中心、七天/备份双记录、自动备份结果提示和脱敏诊断包。
- 稳定性基线覆盖 10 万字、10,000 条搜索、旧数据规范化、SQLite 迁移、磁盘满、revision 冲突、备份恢复和 vault 无明文。
- 验证：前端 37/37、Rust 22/22、TypeScript、oxlint、Vite、Tauri release、NSIS 和发布哈希门禁通过；主入口 218.67 kB。
- 安装包：`apps/desktop/src-tauri/target/release/bundle/nsis/一隅_1.0.0_x64-setup.exe`；SHA-256 `D5EB2A969C141837744D2CD55615E6EA82339779E1978DC1C5E34004E7D9835C`。
- 边界：Windows/IME/缩放/锁屏、连续 7 天和跨机器恢复仍必须由用户真实验收，当前状态不是“已验证”。

### 2026-08-09：0.5.0 工具栏与设置分类补丁

- 插入图片从章节页头移入正文格式工具栏，和引用、分割线保持同一操作层级。
- 设置页重排为四个可折叠类别，默认减少纵向长度，并用本地偏好记住展开状态。
- 后续 Roadmap 重排为 M6 `1.0.0` 长期可用版、M7 `1.2.0` 氛围与陪伴基础、M8 `2.0.0` 伙伴成长与桌面形态；云同步/插件/在线音乐暂不承诺。
- 验证：前端 29/29、TypeScript 与 oxlint 通过；同版本发布门禁将重建 `0.5.0` 安装包和哈希。

### 2026-08-09：0.5.0 M4–M5 合并交付

- 完成图片素材、AI 印象图、完整备份/恢复、开放格式导入导出和作品级加密，并把原两个阶段作为一次完整产品增量交付。
- 安全边界：API Key 使用 Windows DPAPI；备份不含秘密；加密 vault 使用 Argon2id + XChaCha20-Poly1305；锁定态普通资料库和素材目录不保留私密正文/图片。
- 验证：前端 27/27、Rust 19/19、TypeScript、oxlint、Vite 分包、Tauri release 与 NSIS；主入口约 212.32 kB。
- 已知限制：复杂 DOCX/PDF 版式和真实锁屏/休眠需 M6 人工回归；自定义 AI provider 只提供安全配置边界，未绑定未获用户选择的服务协议。

### 2026-08-09：0.2.0 首轮体验反馈补丁

- 修复首页硬编码日期，星期、问候、月、日和年份现在跟随系统时间，持续打开跨日及窗口重新获得焦点时自动刷新。
- 人物、地点、时间线的填写和关联由卡片内扩张改为固定右侧工作栏，资料网格与时间轴尺寸保持稳定。
- 验证：前端 22/22（新增跨午夜日期更新与三类资料侧栏编辑断言）、TypeScript 和 oxlint 通过；同版本安装包重新构建后更新哈希。

### 2026-08-09：0.2.0 M3 交付

- 完成资料与时间线全部阶段范围，并将 M2 结构前置任务一并收口。
- 验证：前端 21/21、Rust 11/11、完整静态检查和 Windows NSIS 构建通过。
- 架构：采用兼容快照 + SQLite v4 关系投影；仓储拆出 migration/projection，避免继续扩大单文件职责。
- 偏差：未把投影表升级为唯一权威写入模型，以降低旧资料迁移风险；该渐进策略已记录为 DEC-005。
